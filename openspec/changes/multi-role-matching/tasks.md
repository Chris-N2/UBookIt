## 1. Core — the assignment

- [x] 1.1 Add the assignment: role slots (a role of count *N* contributes *N*) against candidates, returning a saturating matching or nothing. Keep it a pure function over slots and candidate lists — no store, no clock — so it is testable without a service (design D1).
- [x] 1.2 Augmenting paths (Kuhn's). Deterministic throughout: slots in role order then slot index, candidates in resource-id order, so identical inputs give an identical assignment and a test can assert **which** resources were chosen.
- [x] 1.3 Unit tests from the problem, not the implementation: the case a first-fit walk strands (the resource eligible for both slots must go to the slot with no alternative); a count of 2 against one free candidate; disjoint pools where assignment is trivially the independent choice; an empty pool; and a pool larger than the slots.
- [x] 1.4 **At least one fixture MUST use incomparable capability sets** — `{cert-x}` against `{welsh}`, where neither pool contains the other — and not merely a nested pair. Pools for one resource type are nested exactly when the capability sets are comparable, and `{cert-x}` against `{}` is nested, which is also the shape real configurations usually take ("two therapists, one of whom needs the certificate"). **Greedy assignment is optimal over nested pools**, so a suite built only from the realistic shape would pass a greedy implementation and prove nothing. The defect this change exists to prevent is only visible where the pools cross.
- [x] 1.5 **Mutation-check 1.3 and 1.4 at the threshold, not only the presence.** Three plausible wrong implementations, each to be tried separately: first-fit greedy in role order; greedy sorted most-constrained-first (which defeats a nested fixture and is why 1.4 exists); and dropping the distinctness constraint. Each must turn something red. ⑨-1a shipped a green suite twice; both defects were found this way, not by reading tests.
  - Run at apply, each mutation separately against the real source, then reverted: **first-fit greedy in slot order** → 2 red; **most-constrained-first greedy, recomputed after each choice** → 1 red; **distinctness dropped** → 6 red. The middle result is the one worth recording: most-constrained-first passes `The_resource_eligible_for_both_slots_goes_to_the_slot_with_no_alternative` (the nested shape) and is caught **only** by `Incomparable_pools_defeat_a_greedy_walk_in_either_order`. D5's trap is therefore demonstrated rather than argued — a suite drawn from realistic configurations alone would have shipped that mutation green.

## 2. Core — availability

- [x] 2.1 Recompose service availability from the assignment: a (start, length) is offered exactly when a saturating assignment of distinct resources exists there (design D2).
- [x] 2.2 Build each start's runs from the feasible length set rather than by pairwise run intersection. The set need not be one anchored run, so emit several where needed — `{30, 90}` without 60 must be expressible and exact.
- [x] 2.3 **Differential test against today's answer (design D3).** For services whose roles all name distinct types with count 1, the new composition MUST return exactly what the old intersection returned — same starts, same runs, same order. This is the guarantee that makes the change safe for every service that exists, and it is the one a subtle matching bug would break silently.
  - Two layers. Every pre-existing composite test is a differential test and all of them pass **unchanged** — same starts, same runs, same order. Beside them, `Distinct_type_services_answer_exactly_as_the_intersection_did` runs four granularity configurations against an oracle written out from the *old* algorithm (union within a role, pairwise run intersection across roles), deliberately not sharing code with the thing it checks.
  - **The spec text now says this too** (corrected after QA round 1): the delta's SHALL and the *Distinct-type services are unaffected* scenario said "identical", which is a stronger guarantee than is delivered or tested. `tasks.md` is archived at this change's close, so recording the ruling only here would have left ⑨-2a and ⑩ reading a promise nothing keeps. Both now state equality over **starts offered and lengths denoted**. The single-role count-1 guarantee remains "exactly equal", because the short circuit returns that role's union run for run.
  - **Chris's ruling (recorded at apply).** The oracle compares the **lengths each start denotes**, not the run objects, because rebuilding runs from the feasible length set can express the same lengths more compactly than the pairwise intersection did — `{(60,60,60), (120,120,120)}` becomes `(60,120,60)`. In the event the divergence did not arise: with single-length runs normalised to be anchored on themselves (see below), every existing composite fixture produces byte-identical runs. The weaker comparison remains the stated guarantee because the compaction is possible in principle.
  - Two defects found and fixed while making that hold, both of which a lengths-only comparison would have hidden: a scanned stretch of exactly one length was anchored on the step that found it rather than on itself, so subset elimination's divisibility test could not absorb it and redundant runs were published beside the run already denoting them; and a **single role of count 1** was being recomposed rather than returned as its role's union, which the spec explicitly guarantees is unchanged. The single-slot case now short-circuits to the union, and is deliberately not extended to a single role of count > 1.
- [x] 2.4 Tests for the new ground: a start two same-type roles cannot both fill is not offered; one role of count 2 with a single free candidate offers nothing; two differently-capable same-type roles with one qualifying resource each *is* offered; a start whose only saturating assignment narrows the lengths offers only the narrower set.
  - Mutation-checked: replacing the saturating assignment with "every slot has some eligible candidate" — the over-claim this change exists to remove — turns four of the new cases red, including design D2's `{30, 90}` worked example.
- [x] 2.5 Confirm the single-role guarantee still holds **for count 1**, and that a single role of count 2 is deliberately not covered by it — the old requirement said "a single-role service", which is now too broad.
- [x] 2.6 Cost: no additional I/O, and the existing single batched claims read unchanged. Assert the claims read count in a test rather than assuming it.
  - `InMemoryBookingStore` now counts batched claim reads, and `Spec_scenario_composite_availability_costs_one_claims_read` asserts exactly one over a full day on a 30-minute grid — guarded by an assertion that the query really is a large one, so the count cannot pass on an empty result.

## 3. Core — placement

- [x] 3.1 Resolve a saturating assignment instead of walking the cartesian product. **`Booking.Create` throws on duplicate resource ids**, so an assignment that reuses a resource is not a refusal but an unhandled exception — the injectivity must be structural, not a filter after the fact.
- [x] 3.2 A regression test that a service whose roles share a pool never produces a booking claiming one resource twice, and never *attempts* one.
- [x] 3.3 Preference becomes booking-level (design D4): seek a saturating assignment including the preferred resource, in whichever slot it fits; fall back to any saturating assignment when none includes it; keep `resource-not-eligible` for a resource eligible for no slot. Note the preferred resource is today ordered to the head of **every** pool containing it, which with overlapping pools makes the duplicate the *first* attempt.
- [x] 3.4 Rework the all-fail classification so its unit is a saturating assignment rather than a combination, preserving every existing outcome: request-level failures reported as themselves, claimed-but-would-have-refused not counted as a race, and one admitting candidate not making it `conflict` while a slot cannot be filled at all.
- [x] 3.5 Keep the attempt bound: no growth as the product of pool sizes, the single advisory claims read, and one attempt in flight with locks acquired in deterministic order and released together.
  - Bound is now **linear in candidates**, better than before: each failed attempt excludes at least one resource and the loop stops when it learns nothing. The single advisory claims read is unchanged, and no read was added to the failure path.
  - **Chris's decision (2026-08-16): this stands as-is.** "As long as it's never wrong, this seems fine; the spec exists, let's stick within it as much as we can." So the conservative drop stays and the second claims read is not added — the spec's "single advisory claims read" is honoured rather than argued with, and the exact-behaviour variant waits for a case that makes it necessary. Detail below.
  - **Recorded residue.** A conflict names no resource — the store reports that the placement clashed, not which claim clashed — so a failed attempt whose resources all pass their own rules drops the *whole* assignment. That is conservative: in the window between the advisory read and the attempt it can report `conflict` where a different assignment would still have booked. The alternatives were a second claims read per failed attempt (extra I/O the spec's "single advisory claims read" does not sanction) or enumerating distinct assignments (the exponential the change exists to remove). A false `conflict` in a genuine race is exactly what the advisory read is already permitted to cause. Non-race behaviour is unaffected, and a deterministic refusal still drops only the resource at fault, so the candidate walk picks the same replacement it always did.
- [x] 3.6 Tests: a count claims that many distinct resources; a greedy choice that strands a slot still books; a count exceeding the free resources books nothing and claims nothing; every ⑦-2 and ⑨-1 placement scenario unchanged.

## 4. Core — validation, counts and ordering

- [x] 4.1 `ServiceRole.Count` validated as at least 1 and at most the bound, with its own stable failure code carrying the offending role (design D8). Pick the ceiling at apply and record it.
  - **Ceiling chosen: `ServiceRole.MaxCount = 20`**, with its own code `service-role-count-invalid` against `Roles[i].Count`. A service needing more than a couple of dozen of one resource type is a different kind of product — a hall booking rather than an appointment — and twenty slots is trivial for the assignment either way. Nothing in the algorithm depends on the value, so it stays trivially adjustable. The boundary itself is tested in both directions (20 accepted, 21 rejected), because a test asserting only that 21 fails would pass an implementation that rejected 20 too.
- [x] 4.2 Narrow `service-role-duplicate-type` to reject only roles matching on type **and** capabilities, with the message naming the count as the correction (design D5). Update the code's own documentation, which currently states the restriction as type-only and names this change as the one that lifts it.
- [x] 4.3 A count exceeding the eligible pool is **accepted** — a property of the pool, not the service. Assert it, so nobody later "fixes" it into a rejection.
- [x] 4.4 Give the canonical role ordering a total tiebreak — type, then sorted capability keys, then count (design D6). It is total *only because* 4.2 rejects an exact duplicate, so the two move together.
  - **Noted after QA round 1:** the count term is in fact **unreachable as a tiebreak**. Two roles equal on type and capabilities are rejected by `DuplicateRoleFailures` before `Sort` runs, so the first two terms already decide every pair that reaches the comparer. Design D6 presents all three as needed for totality; two suffice. The term is kept deliberately — it costs nothing, and a comparer that returns 0 for two roles differing only in count is exactly the shape that would reintroduce the unstable sort if the duplicate rule were ever relaxed further — but the reasoning in D6 overstates the case and should not be read as load-bearing.
- [x] 4.5 Test round-trip stability: a service with two same-type roles saved and re-read repeatedly returns them in the same order and compares equal. Vary the supplied order, or the test passes on an accident of insertion order.
- [x] 4.6 Confirm no migration is needed — `Count` is an existing column and the role table has no unique index on `(ServiceId, ResourceType)`. **Verify against the model snapshot, do not assume**, and confirm an integration round-trip of two same-type roles.
  - Verified in `UBookItDbContextModelSnapshot.cs`: `ServiceRoleRow` carries `Count` as an existing `int` column and its only index is the non-unique `HasIndex("ServiceId")`. A snapshot states what EF believes rather than what the database will accept, so the round trip is also run for real — `Two_same_type_roles_and_a_count_round_trip_without_a_migration`, re-read from two separate contexts so a stable order cannot come from a change tracker.

## 5. Core — the alignment check

- [x] 5.1 Skip pairings of equal resource ids in `StartAlignment.Compare` (design D7). Without it, a shared resource silences the report through an assignment that can never happen.
- [x] 5.2 Do **not** skip same-type role pairs wholesale. Test the case that distinguishes them: two roles of one type requiring different capabilities, no resource satisfying both, no shared grid — must still be reported.
- [x] 5.3 Test that two roles over a single-resource pool report nothing (nothing to compare), and record that this is ⑨-2a's insufficiency case rather than a misalignment.
  - Mutation-checked both directions: removing the skip turns the shared-resource case red; replacing it with a wholesale same-type-role skip turns *both* new cases red, including the one that exists specifically to catch that over-correction. The skip also had to go **before** the daylight-saving guard — a self-pairing has `gcd(g, g) = g`, so a granularity not dividing an hour would have cleared the whole role pair by that route instead.

## 6. Management API and editor

- [x] 6.1 Carry `Count` on the management role model in both directions, and stop the editor hard-coding 1.
- [x] 6.2 Add a count control per requirement row: labelled, associated with its row as the other controls are, defaulting to 1, with server failures rendered against the offending row.
- [x] 6.3 The editor still must not enforce the duplicate rule (⑨-1 design D1) — it stays a server rule so changing it stays a change in one place.
- [x] 6.4 Accessibility: the count control is labelled within the row's fieldset, its failure is associated, and every referenced id resolves in the same shadow root. Verify by reading the shadow DOM, not from screenshots.
  - Built with its own `label[for]`, its own hint id and its own error id — deliberately not sharing the type control's, which would read the type's failure out against the count.
  - **Static half done** (mechanical parse of the row template): both `label[for]` targets are produced as ids on their own inputs; the two `aria-describedby` chains reference *disjoint* pairs — type → `resource-type-hint-{i}` / `err-requirement-{i}`, count → `role-count-hint-{i}` / `err-requirement-count-{i}`; all four id variables are index-suffixed so rows cannot collide; and the count's error id is referenced only when a count error exists, so it never dangles.
  - **Live pass done** (Chris logged in; read out of the rendered shadow DOM via `javascript_tool`, never from screenshots — a picture cannot distinguish a resolving `aria-describedby` from a dangling one, which is the fault ⑦a's live pass caught).
  - **Healthy state**, `MRM Joint Session` (two same-type roles, so two rows): each count input carries a unique id (`role-count-0` / `role-count-1`), exactly **one** `<label for>` resolving to it ("How many at once"), `aria-describedby` → its own `role-count-hint-N` which resolves inside the same shadow root, and **no id shared with the type control**. Each row sits in its own `<fieldset>` with legend "Requirement 1" / "Requirement 2". `aria-invalid` is **absent, not empty** — the ⑨-1a distinction.
  - **Server-rejection state** — the half a static parse cannot reach. Setting row 2's count to 0 and saving: `aria-invalid="true"` appears on **that row's count input only**, its `aria-describedby` grows to `role-count-hint-1 err-requirement-count-1`, and **both ids resolve**, the second to "A service role count must be between 1 and 20." Row 1 is untouched — no `aria-invalid`, no error, describedby unchanged — so the failure landed on the row that caused it rather than the first, which is exactly what duplicated ids would have broken while looking perfectly normal on screen. The error summary is populated and no form data is lost.
  - **Each control gets its own message.** Making the same row's *type* invalid instead put `aria-invalid="true"` on the **type** control with `err-requirement-1` in its describedby, while the count control stayed clean — proving the two controls do not share an error element, which was the specific risk in adding a second control to an existing row.
  - **Round-trip**: `MRM Workshop`, saved with count 2 through the API, opens in the editor showing **2** — not the hard-coded 1 the editor used to send.
  - The `MRM Joint Session` fixture was restored to its original state afterwards and saves cleanly.
- [x] 6.5 Regenerate the client against the running TestSite.
  - Regenerated against the running TestSite. **Diff is empty** apart from line endings — as expected, since `count` has been on the management role model since ⑥ and the delivery API is not in the `ubookitbackoffice` swagger group. `count: number` is present and **required** (not optional) on the generated role models, so the editor's `role.count ?? 1` on load is belt-and-braces rather than load-bearing. All 14 service/resource operations still present; no unrelated drift. File restored to keep the tree clean.

## 7. Delivery API

- [x] 7.1 Publish `Count` on the role read model — always present, 1 where a role needs one resource, never absent-as-default.
- [x] 7.2 Confirm the deterministic role order survives two roles sharing a type, across repeated reads.
- [x] 7.3 Check the change against the standing "eligibility remains derivable from public reads" guarantee: count is not an eligibility input, but publishing it is what stops a consumer computing pools correctly and still misreading what the service needs.
  - Checked and unchanged. Count is **not** an eligibility input — a role of count 3 draws on exactly the pool a role of count 1 does — so publishing it discloses nothing the resource and service reads did not already imply, and the `resource-not-eligible` failure's disclosure argument is untouched. What it fixes is the other half: the pools were already computable, but what the service *required* of them was not.

## 8. Verification

- [x] 8.1 Full solution build with `--no-incremental`, TestSite stopped first. Only the known NU1903 advisories are acceptable.
  - Clean: `Build succeeded`, **76 NU1903 advisory lines and nothing else** — no compiler, analyzer or TypeScript warnings. 76 matches the ⑨-1a baseline.
- [x] 8.2 Full unit, integration and client runs green, including every ⑤/⑥/⑦/⑧/⑨-1/⑨-1a scenario unchanged.
  - **573 unit / 54 integration / 28 client** (corrected after QA round 1 — the figure first recorded here was 582, taken before the nine `LengthRun.TryIntersect` tests were removed with the method itself in a later commit). Every pre-existing placement and composite-availability scenario passes without being edited, which is the differential guarantee in its strongest form: the only tests this change had to rewrite were the four that asserted the *rejections* it lifts.
- [x] 8.3 Live backoffice verification: a service with two `therapist` roles differing in capabilities saves; a role with a count of 2 saves and round-trips; two identical roles are rejected with a message naming the count.
  - Verified against the running TestSite over the authenticated management API (the ⑥ technique — credentials read from the `umbraco-mcp-ubookit` entry, never printed), so no backoffice login was needed. Fixtures: three `therapist-mrm` resources with **incomparable** capability sets — `MRM Ana {cert-x}`, `MRM Bea {welsh}`, `MRM Cai {cert-x, welsh}` — which is the shape where greedy actually fails; nested sets would not have exercised it.
  - **a** two roles `{cert-x}` / `{welsh}` → **200**, both roles round-trip.
  - **b** one role, count 2 → **200**, re-read returns `"count":2`.
  - **c** two identical roles → **400** `service-role-duplicate-type`, field `Roles[1].ResourceType`, message *"Two roles require resource type 'therapist-mrm' with no required capabilities. **Use a single role with a count instead.**"* — the correction named on the wire, not only in the domain.
  - **d** count 0 → **400** `service-role-count-invalid`, field `Roles[0].Count`, *"A service role count must be between 1 and 20."*
  - Both problem-details bodies carry the `type` member, so Umbraco's backoffice error interceptor will keep them rather than replacing the payload with a generic fatal-error message (the ⑦a gotcha).
- [x] 8.4 Live: book a service whose roles share a pool and confirm the booking carries two distinct resources; confirm a start only one resource can fill is not offered.
  - **a** Booked `MRM Joint Session` (roles `{cert-x}` and `{welsh}` over the overlapping pool) through the anonymous delivery API → **200**, one booking carrying **two distinct** resources: Ana into the `cert-x` slot, Cai into the `welsh` slot. Ana can fill only the first, so this is the assignment placing the shared resource where the other cannot go — not an arbitrary pair.
  - **b** A role of `{cert-x}` with count 2 resolves to exactly `{Ana, Cai}`. Booking Ana directly at one start removed **that start alone** (6 → 5); no other start was lost.
  - **The control that makes (b) mean anything**: an otherwise identical service with count **1** still offers that same start, because Cai is free there. So the count-2 service lost it because one resource cannot fill two slots — the assignment refusing — rather than because the pool was empty. Without this control the check could not tell those apart.
- [x] 8.5 Stop the TestSite and check port 44348 for orphaned processes.
  - Stopped; no `UBookIt.TestSite` process remains and nothing is LISTENING on 44348. (A `TIME_WAIT` entry against 44348 lingers with PID 0 — an outbound socket from this session's own HTTP client, not an orphaned server.)
  - Restarted afterwards for 6.4's live pass, then stopped again and re-checked.

## 9. Spec hygiene — this change replaces requirements

- [x] 9.1 **Run the guarantee diff `CLAUDE.md` requires for every MODIFIED requirement.** Unlike ⑨-1a, this change genuinely replaces six requirements across three specs. For each: list every SHALL and every scenario in the current main spec, then decide explicitly whether it is carried forward, deliberately dropped (stated as a removal with its reason), or superseded by a stronger claim. A carried-forward guarantee needs a scenario, not an assumption.
  - Run at apply, mechanically then by hand, over all nine modified requirements (the proposal said six; the delta touches nine). Scenario sets diffed first, then every SHALL sentence of each replaced requirement matched against the new prose.
  - **Prose: clean.** Every SHALL in the current main specs is carried forward, reworded for slots, or deliberately superseded with the supersession stated in the delta — including the four that had to go: independent per-role choice (the premise the change removes), the combination as the unit of classification, preference as an ordering hint on one role, and `count SHALL be 1`.
  - **One scenario was silently lost, and has been restored.** `Intersection distributes over each role's union` was dropped as mechanism, but its second clause was a guarantee about the *answer* — "the set of lengths offered equals the set of lengths some resource of every role can provide" — and the delta carried that guarantee in prose ("composed over the **sets** of lengths the roles denote, never over their outermost bounds") with no scenario. Added `A role offering several runs is composed candidate by candidate`, restricted to disjoint pools, which is where the clause is still true. This is exactly the failure mode `CLAUDE.md` describes: nothing in the diff looked like a deletion. The behaviour was already covered by a passing test; only the spec had lost it.
- [x] 9.2 Pay particular attention to "Booking a service resolves a resource by candidate loop" — 15 guarantee paragraphs and 12 scenarios, most of which are about claims-exclusion and failure classification and must survive the rewrite untouched.
- [ ] 9.3 The outward grep for sibling specs this change falsifies, **before and after** the sync's own edits. One candidate is already known: `service-booking`'s "Every bookable length run is anchored at its step" justifies itself partly by *"intersection across roles relies on both grids being anchored at zero"* — if composition no longer intersects runs across roles, that sentence needs revisiting even though the anchoring property is still required by subset elimination.
  - **CORRECTED after QA round 1.** The first pass recorded three findings and was materially incomplete — QA found six more, and I re-ran the grep independently and confirmed all six. The complete list is **nine**, all in requirements this change does NOT modify, all to be applied at sync time and the grep re-run afterwards. Two further grep hits were checked and are false positives (`resource-management:69` "distinct resource type keys" means unique keys; `delivery-api:254` "distinct endpoint"), and `service-booking:404` sits inside `Overlapping eligibility pools`, which the delta already modifies.
  1. `service-booking` / *Every bookable length run is anchored at its step* — the pre-recorded candidate, confirmed. "intersection across roles relies on both grids being anchored at zero" is now false: composition does not intersect runs across roles. The **property is still load-bearing**, so the requirement stays — subset elimination still assumes equal-step runs are in phase, and the new step scan and its single-length normalisation both depend on anchoring. Only the justification needs rewording.
  2. `services` / *the ⑧a configuration-preview requirement* — "Because every role names a distinct type, each role's chain SHALL be independently true: no role can consume a resource another role's chain counted." **Falsified outright.** Two roles of one type now draw on overlapping pools, so a resource can be counted in two chains. The conclusion it supports — do not combine several roles into one count — survives and is in fact *strengthened*: a combined number would now double-count. The justification must be replaced, not deleted. Note this is a readout an editor reads, so ⑨-2a's pool-sufficiency diagnostic inherits it.
  3. `bookings` — "since service placement attempts **combinations** in sequence and depends on a failed attempt leaving no state." The all-or-nothing guarantee is untouched and still depended upon; the sentence names the mechanism that no longer exists. Reword to assignments.
  4. **`service-booking` / *All candidates failing reports one of two distinct outcomes* (lines 329, 331, 353) — the serious one, and not merely stale prose.** It states the classification rule as *"The unit of an attempt is a **combination** — one candidate per role"* and *"which requires **every role to have a candidate whose own rules admit the request**"*. The modified sibling requirement explicitly supersedes that (*"The unit of that classification SHALL be a saturating assignment, not a candidate and no longer a combination"*), and the implementation's own comment says why the old test is wrong: a role of count 2 with a single admitting candidate passes it and still cannot be filled. **After sync the spec would carry two requirements stating contradictory rules, and the unmodified one would be the wrong one.** Its scenarios at 291/299/353 need the same treatment.
  5. `service-booking` / *Union availability over a candidate pool* (line 126) — "Across roles it is an **intersection** … for a **single-role service** the two coincide, and this requirement describes that case unchanged." Both halves now false: composition is not an intersection, and the coincidence holds only for count **1**.
  6. `service-booking` spec purpose (line 4) — "availability as the union … **intersected across the roles**, and **candidate-loop** placement … each attempt claiming **one resource per role**."
  7. `delivery-api` / *Service bookable-start read* (line 213) — "Across roles it arises from **intersecting** the runs of one candidate per role."
  8. `services` spec purpose (line 5) — "one or more required roles, each naming a **distinct resource type**."
  9. `resource-management` / *Service configuration preview endpoint* (line 171) — "**Saving such a service is rejected**, but previewing one is how an editor sees…". Saving is now accepted where capabilities differ. Note this is a **third** spec: the first pass caught the equivalent sentence in the `services` spec and missed that the same claim is made again here.
  - **Why the first pass missed six.** It grepped for the vocabulary of the *change* (distinct type, disjoint, count) rather than for the vocabulary of the *mechanisms being replaced* (combination, intersection, candidate loop, one resource per role). The second sweep searched both and is recorded above; a third sweep for the same terms across all specs found nothing further.
  - Also surfaced: `LengthRun.TryIntersect` had **no production caller** once composition stopped intersecting runs. **Chris's decision (2026-08-16): dropped**, having established it is redundant rather than merely unused — nothing planned needs it (⑨-2a derives a deficient set, ⑩ is a UI), and external consumers never had access anyway, since `LengthRun` crosses the wire as `LengthRunModel` (three ints) rather than as the type. Nothing is published yet, so removing it now is free where removing it later would not be. Re-adding is fifteen lines fully determined by the anchored-run invariant, and git holds it.
  - Removing it improved the differential test rather than weakening it. The oracle had been **calling the production helper**, so a wrong helper would have made the oracle wrong in exactly the same way — the one thing an oracle must not do. It now computes the intersection itself, and still agrees.
  - `IsSingleLength` was checked at the same time and **kept**: it has no production caller either, but it is a one-line predicate a test reads with, not fifteen lines of subtle arithmetic. Different category, different call.
  - The `LengthRun` class documentation justified the anchoring invariant partly by that intersection. Corrected in the same commit — the property is still load-bearing, now for subset elimination and for the step scan that rebuilds runs, which is the same stale-justification shape this grep found in the spec.
- [x] 9.4 Confirm the two singular-drift items ⑨-1 deferred are discharged here, since this change legitimately modifies both requirements: `service-booking`'s "the service's role" and `services`' row/summary singulars.
  - To be confirmed against the synced text at sync time; the delta prose for both requirements is written in plural terms throughout.

## 10. Handover

- [x] 10.1 Record what ⑨-2a inherits: the assignment's deficient-set witness is what makes both the pool-sufficiency diagnostic and the richer `service-unavailable` message possible, and both should be derived from it rather than recomputed.
  - **The witness is not yet produced.** `SlotAssignment.TrySaturate` returns `null` rather than the deficient set, because nothing in ⑨-2 consumes one and inventing a shape with no consumer is the liability design D1 argues against. Producing it is a small, local change: when a slot's augmenting walk fails, the resources reachable from it are exactly a deficient set's neighbourhood, and the slots that reach only those are the deficient set. That is a return-type change on one function, not a redesign — which is the whole reason the algorithm was kept behind one function.
  - ⑨-2a should derive **both** its outputs from that one witness rather than recomputing: the configuration-time pool-sufficiency diagnostic ("this service needs two therapists with `cert-x` and only one exists") and the richer `service-unavailable` message ("two therapists are needed and only one is free then") are the same deficient set asked at configuration time and at placement time. Recomputing either separately would be a second implementation of the rule, free to disagree — the fault ⑧a design D1 and ⑨-1's classifier seam both exist to prevent.
  - ⑨-2a also inherits the alignment check's deliberate silence: two roles over a single-resource pool now report **no** misalignment (correctly — they are short of resources, not misaligned), and nothing yet says so. That is the clearest case the pool-sufficiency diagnostic exists for, and there is a test naming it.
  - It further inherits the ⑧a preview requirement's falsified sentence recorded at 9.3 — the readout still justifies itself by roles naming distinct types, and it is the surface the diagnostic lands on.
- [x] 10.2 Record whether the count ceiling chosen at 4.1 proved right in practice, and the ⑩ questions this change leaves open — per-role preference, and whether direct resource booking survives.
  - **Ceiling (20) was never load-bearing during apply.** Nothing in the assignment, availability or placement paths scales with it in a way that made the value matter, and no test needed it to be larger or smaller — only the boundary cases assert it at all. It remains a validation bound rather than an algorithmic one, so changing it later costs a constant and a message.
  - **Per-role preference is now the only preference question left open.** ⑨-2 settled the booking-level reading (design D4) and the request contract is unchanged, so ⑩'s "pick who" UI is free to introduce a per-slot shape without undoing anything: `SlotAssignment.TrySaturateIncluding` already pins a resource to a *chosen* slot internally, so a per-role preference is a parameter away rather than a rewrite.
  - **Direct resource booking** is untouched and its own requirement still passes unchanged. Nothing in ⑨-2 makes the decision easier or harder, so [[ubookit-direct-resource-booking-question]] stands exactly as it did — still best decided at ⑩, still the last cheap moment.
  - **One residue this change created**, recorded so ⑩ or QA can settle it rather than rediscovering it: on a lost race, placement drops the whole failed assignment because a conflict names no resource (see 3.5). It is conservative and never wrong, but a second targeted claims read on the failure path would make it exact, and that is a spec question about "the single advisory claims read" rather than a code question.
