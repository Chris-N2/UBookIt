## 1. Baseline and reproduction (gate: do not change the test until 1.3 holds)

- [x] 1.1 Branch `stable-cost-measurement` from `main` (`afc84bb` or later). Record the unit baseline (1980 on `main`), running per project, sequentially, with the client built first. Verify: the count is recorded here, with the command used.
    - Branched at `afc84bb`. `npm run build` (client), then `dotnet build UBookIt.slnx -c Release` (0 warnings), then `dotnet test tests/UBookIt.Tests -c Release --no-build`: **1980 passed, 0 failed, 0 skipped**.
- [x] 1.2 Build the throwaway harness from design D6 in the session scratchpad, **never in the repo**. It runs the CURRENT measurement (sequential contiguous means) at least 200 times in one process under a load generator: allocating and spinning threads at about 2× the core count. Verify: the harness prints each run's ratio and a failure count; `git status` shows nothing new under the repo.
    - The harness references the Release `UBookIt.Tests.dll` and runs the old method verbatim, alongside candidate methods, in the same trial loop. **Load deviation:** 2× cores (32 threads) made the run too slow to finish in 10 minutes, so **8 allocation threads** (churn with survivors, which forces gen-2 collections) on 16 cores. The maintainer was also playing a game throughout, which is extra, uncontrolled load and only makes the test harder to pass.
- [x] 1.3 **GATE:** the current method fails (ratio ≥ `days × 4`) in a measurable share of runs under load. Record the rate and the load settings. Verify: the numbers are recorded here. **If it cannot be made to fail, STOP and report to the maintainer**: the mechanism is unproven and the change does not proceed as designed.
    - **MET.** No load: 0/50 fails, ratio 14–34×. 8 load threads: **86/200 fails, twice** (p50 88.9× / 98.5×, max 4513× / 3889×). Failures also run the other way: the minimum ratio fell to 0.1×, so the old method is dominated by whichever loop the stall hits.
    - **The design as written FAILS its own bar.** Wall-clock interleaved ABBA with a minimum per call: K=4/Kone=4 gave **48/200** failures; equal-duration batches (K=4, Kone=40) gave **23/200** and then **51/200**. Pauses that come more often than a batch lasts leave no clean window batch, and the shorter gaps between them favour whichever batch fits. The first design's D1/D2 reasoning ("load long enough to spoil every window batch also spoils the one-day batches") is false for that shape of load.
    - **Thread-CPU-cycle timing** (`QueryThreadCycleTime`), with the same sampling and K=4/Kone=40/R=25: **0/200** under the same load, p50 24.7× (no load: 25.4×), max 41.2×; **0 thread hops** inside any batch. **The design needs updating before 2.x. Paused for the maintainer.**

## 2. The measurement

- [x] 2.1 Add the thread-CPU-time source (design D2): `QueryThreadCycleTime` on Windows, `clock_gettime(CLOCK_THREAD_CPUTIME_ID)` on Linux (3) and macOS (16), and a failure naming any other platform. Put it in the test project only. Rewrite the test body to use interleaved ABBA rounds: 4 window calls against 40 one-day calls per batch, 25 rounds, minimum per call (D1, D4). Keep the warm-up per width, the busy seven-day resource, the widths and the assertion `ratio < days * 4` **unchanged** (D5). Verify: `git diff` shows the threshold expression and the widths untouched; the test passes when run alone.
    - `tests/UBookIt.Tests/Support/ThreadCpuTime.cs` (new) + test rewritten. `ratio < days * 4`, `horizonDays: 90`, the busy resource and the per-width warm-up are unchanged. Release build 0 warnings. Alone ×3: **24.7×, 21.7×, 23.5×** (cycles), all passing.
- [x] 2.2 Add the thread-switch guard (D3): discard a batch whose managed thread changed, and fail with a "could not measure" message if fewer than 10 of 25 batches are valid for either width. **Prove both failure paths fire:** temporarily force every batch to count as a switch, then temporarily force the unknown-platform branch; each must fail with its own message. Revert both. Verify: both messages are recorded here, and the test passes after the revert.
    - Forced discard (`&& false` on the same-thread check): FAILED — *"Could not measure: only 0 one-day and 0 window batches of 25 stayed on one thread (need 10 each). The read has started completing asynchronously, so a per-thread CPU clock cannot time it."*
    - Forced unknown platform (`&& false` on all three `OperatingSystem.Is*` branches): FAILED — *"System.PlatformNotSupportedException : No per-thread CPU clock is wired for Microsoft Windows 10.0.26200. Add one to ThreadCpuTime rather than falling back to wall-clock time."*
    - Both mutations reverted from backups; `grep -c '&& false'` = 0 in both files.
    - **SUPERSEDED by 2.7 (QA round 1 CRITICAL).** The first proof forced the message path and never made the read async, so it proved the mechanism, not the guarantee. The thread check it proved could not see a genuinely async read.
- [x] 2.3 Keep the `[available-dates]` line, reporting the minimum-based figures in the platform's units and the valid batch count for each width. Update the class remarks to describe what is measured and why: wall-clock time counts stalls the thread never ran through. Verify: the line appears in detailed console output, and its batch counts are recorded here.
    - e.g. `[available-dates] one day: 37783 cycles | 30 days: 932256 cycles | ratio: 24.7x | per-day: 31075 cycles | batches: 25/25 one-day, 25/25 window`. **25/25 batches on both widths in all three runs**: the xUnit 2 synchronization context did not move any batch to another thread.
- [x] 2.4 Put the final measurement code into the harness verbatim and run it on Windows under the SAME load (8 threads, 200 trials) beside the old method. Verify: **0/200** failures for the new method, recorded beside the 1.3 figures.
    - Stronger than verbatim: the harness calls **the real test method** (`new AvailableDatesCostTests().The_window_read_…()`) from the rebuilt Release `UBookIt.Tests.dll`, catching any assertion failure, and parses its `[available-dates]` line.
    - No load, 20 trials: 0 fails, p50 23.2×, max 25.9×.
    - **8 load threads, 200 trials: REAL TEST 0/200** (min 13.9×, p50 23.8×, p90 30.8×, p99 37.7×, max 40.8×). **No trial discarded a batch** (25/25 on both widths in all 200). OLD method in the same run: **88/200** (p50 94.1×, max 4368.6×). The game was still running.
- [x] 2.5 Linux proof (D6): run the harness in a Docker .NET 10 SDK container. First with no load, where the CPU-time ratio must match the wall-clock ratio (about 25×; this is the sanity check on the native signature and clock ID). Then under 8 load threads, 200 trials. Verify: the sanity ratio and **0/200** failures are recorded here; the old method's failure count in the same run is recorded too.
    - Container `mcr.microsoft.com/dotnet/runtime:10.0` (Ubuntu 24.04.5, 16 CPUs, **1.96 GB VM**), with the Windows-built Release `UBookIt.Tests.dll` and harness mounted read-only. The harness calls the real test method. The game was shut down for these runs.
    - **Sanity, no load, 30 trials:** clock `ns`; real test p50 **21.9×** against the old wall-clock p50 **21.5×**, so the `clock_gettime` signature and clock ID 3 measure real work. 0 fails, 0 discarded batches.
    - **The Windows load does not fit the VM.** Run 1 (the 1.3 load) was OOM-killed (137) after about 80 trials; run 2 (`LOAD_KEEP=1024`) was OOM-killed before trial 20; run 3 (plus `DOTNET_GCHeapHardLimit=1 GB`) crashed with the runtime's "Out of memory." (139) after 20 trials. The large-object-heap arrays in the load are the likely cause.
    - **Run 4, bounded load** (8 threads, allocations 64 B–8 KB so nothing reaches the LOH, keep 4096, live set at most about 256 MB): **REAL TEST 0/200** (min 19.6×, p50 24.2×, p90 26.8×, p99 29.3×, max 30.3×), **0 trials with a discarded batch**. OLD in the same run: **98/200** (p50 117.3×, max 279.3×). A different load from Windows, but the same bar met on both platforms.
    - **UNEXPLAINED, recorded and not dismissed:** in run 1 the real test failed **once**, between trials 41 and 60, while memory was being exhausted. The harness did not yet print failures as they happened, so the message was lost when the process was killed. The likely cause is an `OutOfMemoryException` counted as a failure, but that is not shown. Rerunning run 1's configuration with inline failure printing to capture it.
    - **CAPTURED, and it was NOT an OOM exception; it was a genuine ratio failure.** Rerun of run 1's configuration, trial 26: `one day: 4042 ns | 30 days: 757628 ns | ratio: 187.5x | batches: 25/25 one-day, 25/25 window`, `gc: gen0=2380 gen1=2379 gen2=101 heap=2391 MB`, OOM-killed (137) shortly after. Normal Linux figures for comparison: no load, one day 2.9–4.7 µs and window 67–96 µs; bounded load (heap about 400 MB), 2.6–3.0 µs and 65–72 µs. **The window side inflated about 10× in its CHEAPEST batch.** Nearly every collection was gen 1 over a heap beyond the VM's physical memory, and the measuring thread ran them on its own CPU time. The design's claim that the minimum absorbs this is false at memory exhaustion.
    - **Maintainer's decision (2026-09-24): accept as a known limit and make it diagnosable** (design D4 and the first Risk). See 2.6.
- [x] 2.6 Add the process's GC collection counts (gen 0/1/2) and heap size to the ratio failure message, and state the memory-exhaustion limit in the class remarks. This is new code, so re-prove it: the test passes alone; the mutation from 3.1 fails again with the new message showing the GC figures (then revert, `git diff -- src/` empty); a brief harness rerun on Windows and on Linux under the bounded load, 0 real-test failures. Verify: the new message and the rerun figures are recorded here.
    - The message is **conditional**: "If that heap is near the machine's memory, collections run on the measuring thread were charged to it (see the class remarks); if it is small, the algorithm changed." An unconditional "this is memory" would have misdirected a real regression.
    - Alone: passes, 22.8×, 25/25 batches both widths. Release build 0 warnings.
    - Mutation (same quadratic re-projection): **FAILED 546.8×**, message: *"… Numbers: one day 10028 cycles, window 5483275 cycles. Process GC at failure: gen0 59, gen1 4, gen2 2, heap 18 MB. If that heap is near the machine's memory, … if it is small, the algorithm changed."* It correctly points at the algorithm. Reverted; `git diff -- src/` = 0 lines.
    - The harness's copied `UBookIt.Tests.dll` / `UBookIt.Core.dll` were checked by SHA-256 against the test build output before each rerun (**same=True**), because a stale copy would silently measure old code.
    - **Windows, bounded load (8 threads, `LOAD_MAX=8192`), 100 trials, game off:** REAL TEST **0/100** (p50 25.6×, max 32.4×), 0 discarded; OLD 40/100.
    - **Linux container, same load, 100 trials:** REAL TEST **0/100** (p50 24.2×, max 32.3×), 0 discarded; OLD 33/100.
- [x] 2.7 **QA round 1 (REJECT) — CRITICAL: the thread check could not see an async read.** The reviewer added `await Task.Delay(1).ConfigureAwait(false)` in `GetBookableStartsAsync`: the test PASSED (4.3×, 3.7×, 2.3×), with window batches 25/25 "valid". Adding the quadratic too, it **PASSED at 33.9×, 57.7× and 34.4×**. The projection ran in a pool-thread continuation, and xUnit's synchronization context returned the batch to its starting thread. Fix: check the real precondition, that **each call's task `IsCompleted` when returned** (design D3 rewritten), with the message, Goals and proposal wording brought into line. Treated as new code and re-proved by making the read GENUINELY async (Windows, real test under `dotnet test`, each mutation reverted afterwards):
    - **A** `Task.Delay(1)` on every call: **FAILED ×3**, *"Could not measure: only 0 one-day and 0 window batches of 25 completed synchronously (need 10 each). The read has started completing asynchronously, so part of its work runs on other threads, which the calling thread's CPU clock cannot see."*
    - **B** A plus the quadratic (the reviewer's passing case): **FAILED ×3**, "Could not measure", 0/0.
    - **C** `Task.Delay` on 1 call in 50, plus the quadratic: **FAILED ×3**: once on the ratio (**537.1×**, from the batches that stayed synchronous) and twice with "Could not measure" (8/25 and 9/25 one-day valid).
    - Synchronous quadratic (3.1's mutation) re-run against the new `BatchAsync`: **FAILED 528.4×**.
    - **D** the quadratic behind a blocking `Task.Run(...).GetAwaiter().GetResult()`, **expected to pass as a limit: it FAILED** (440.9×, 483.5×). The runtime ran the queued task inline on the waiting thread. My first draft of the new remark claimed this case was "NOT caught", which was false, and was corrected before commit.
    - **E** the quadratic spread with `Parallel.For`: **106.0× (PASSED), 134.5×, 133.1×** (failed), about a fifth of the synchronous ~530×. **This is the real blind spot:** work on other threads inside a synchronous read. It is stated as a measured limit in the class remarks and in design Risks, not covered.
    - After all reverts: `git diff -- src/` = 0 lines; Release solution build 0 warnings; the test alone passes 23.6×, 25/25 both widths; unit **1980/0/0** (25 s).
    - Harness rerun with the new code (DLLs hash-checked): **Windows 0/100** (p50 25.5×, max 27.6×), **Linux 0/100** (p50 23.9×, max 28.1×), 0 discarded on either; OLD 40/100 and 45/100.
    - MINOR fixed: task 1.2 cited D5 for the harness (now D6). NIT (GC figures are process-wide and cumulative): accepted as is; the message labels them "Process GC at failure" and phrases them conditionally. MINOR (10.3 closed on two derived checks): **for the maintainer**.
- [x] 2.8 **QA round 2 (APPROVE, conditional on two text fixes; the code was accepted).** The reviewer verified B (FAILED ×3, "could not measure"), D (FAILED ×3, 289–326×) and the synchronous quadratic (341–364×); `src/` clean; build 0 warnings; unit 1980.
    - MINOR 1: the guard's justification ("a task already complete when returned ran all of its work here") was false, disproved by the change's own case E, and it contradicted the limit stated beside it. It is now narrowed in the `BatchAsync` remarks and design D3: completion on return means the clock reads bracket everything THIS thread did and never mix two threads' clocks; it does NOT prove the work happened here. D3's opening sentence, which called "every call's work ran on this thread" the checked precondition, is corrected too.
    - MINOR 2: the blind spot was understated. The reviewer's shape `Parallel.For(0, days, …)` **PASSED 3/3 at 60.2×, 64.8×, 54.6×**, against 341–364× synchronous on that day. The class remarks and design Risk 2 now say "55–135× depending on its shape … in one shape passed every run", and describe it as a blind spot, not a near miss. **The `05ecdfb` commit message's "passed one run in three" is superseded by this record.**
    - NIT: case D is inline only when the caller is a thread-pool thread (reviewer's scratch program: 400/400 inline from the pool, 0/400 from the main thread). Added to the remark and the Risk.
    - **QA round 3 (text only, `e2162b4`): APPROVE WITH NITS, no further round.** The reviewer verified that no executable code changed (the test file's diff is `///` comments only); build 0 warnings; unit 1980/0/0; `openspec validate` passes; `git status` clean. Both nits are fixed: "as under xUnit here" is now marked **inferred** (case D was counted in full), and 290–326× is corrected to **289–326×**.

## 3. The guard can still fire

- [x] 3.1 Mutation: temporarily make the projection in `AvailabilityService` quadratic, for example by repeating each day's work once for every day in the range. Verify: the new test FAILS, with the ratio it reported recorded here.
    - `GetBookableStartsAsync` re-ran `Project(context)` once per day in the range. **FAILED at 721.6×** (one day 37,777 cycles, window 27,260,813), about 24 × 30 as expected, with 25/25 batches valid, so it failed on the shape and not on "could not measure".
- [x] 3.2 Revert the mutation. Verify: `git diff -- src/` is empty and the test passes again.
    - `git diff -- src/` = 0 lines. The test passes again at 22.6×, 25/25 batches on both widths.

## 4. Suite and both lines

- [x] 4.1 Full sequential run on `main` (client first; unit, integration, rendering, client). Verify: counts match baseline (unit 1980, integration 191, rendering 1168, client 335), 0 skipped, Release build 0 warnings.
    - On the branch: `dotnet build UBookIt.slnx -c Release` **0 warnings**; then per project with `--no-build`: unit **1980**/0 failed/0 skipped, integration **191**/0/0, rendering **1168**/0/0; `npm test` client **335** (15 files). Unit is unchanged at 1980 because the test was rewritten, not added.
    - **Unexplained, recorded:** the unit run took **1 m 37 s** against **20 s** at the 1.1 baseline. The cost test itself runs in about 120–175 ms, so it is not the source. The maintainer's game was running throughout. Re-timed at once: **1 m 7 s**, then **40 s** with a TRX log. The slowest tests in that run were `PackageCompositionTests…client_is_built_by_the_build…` (25.7 s, shells out to `dotnet pack`) and repo-scanning tests (`PrivacyNoticeSourceTests`, `SensitiveDataRedactionTests`, `HolidaySeamProofTests` and others, 11.8–13.6 s each). None is touched by this change, and the cost test is not among them. The variance run to run (40 s to 97 s, same binaries) is the machine, not the change. **Confirmed:** after 2.6, with the game shut down, the unit suite ran **1980 passed / 0 failed / 0 skipped in 26 s** (baseline 20 s). Integration, rendering and client were not re-run after 2.6: only the unit project's files changed, and the Release solution build was 0 warnings.
- [x] 4.2 Commit on the branch; the maintainer pushes and opens a PR into `main`. Verify on that PR's run: the `pull_request` event fired, the concurrency group is `ci-refs/pull/N/merge`, the parity step ran with `-Line main`, and the run is green. Record this as closing `continuous-integration` 10.3 (here, since the archived change is not edited).
    - Commits `fbd7c0b` (fix) and `5e0ca70` (artifacts). The maintainer pushed the branch and opened **PR #1**, the repository's first PR. Before this, all merging had been local.
    - **PR run `36045165846`: event `pull_request`, conclusion success**, on head `5e0ca70`; `Test`, `Client - tests` and **`Parity with the other published line`: success (it RAN)**.
    - **Push run `36045037049`** (feature branch, same SHA): success, with the parity step **skipped**. Together these show the `github.base_ref` branch of the step's condition firing for the PR and not for the feature-branch push.
    - **Concurrency group:** both runs were `in_progress` at the same time. The branch's group has `cancel-in-progress: true`, so they were in different groups. The PR's group, `ci-refs/pull/1/merge`, is **derived** from the workflow expression (`format('ci-{0}', github.ref)`); the API does not report it.
    - **`-Line main`:** **derived**, not read. The argument is `${{ github.base_ref || github.ref_name }}` and the PR's base is `main`; job logs need admin authentication. The cost test's printed Linux figures on CI were not read, for the same reason.
    - **`continuous-integration` 10.3 is CLOSED by this run**, with the two derivations above stated as such.
    - **Maintainer's decision (2026-09-24), after QA flagged the derived checks:** the maintainer read PR #1's run log, where the parity step reported the CI definitions **identical to `origin/dev/v18`**, and **accepted 10.3 as closed**. That is direct evidence that the step ran for the PR and compared against the other line.
- [x] 4.3 QA review (subagent), then merge the PR only after approval.
- [ ] 4.4 Cherry-pick onto `dev/v18` in the same sitting. Run the unit suite there (baseline 1991). Verify: the counts match, the `AvailableDatesCostTests.cs` file is identical between the lines (`git diff main dev/v18 -- tests/UBookIt.Tests/AvailableDatesCostTests.cs` is empty), and the `dev/v18` push run is green.
