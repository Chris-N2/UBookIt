## 1. Branch and baseline

- [x] 1.1 After the maintainer has pushed the proposal commit, branch `usable-privacy-link` from `main`; record `main`'s SHA and a clean-build baseline (unit / integration / rendering / client), and confirm `SiteSettingsTests` is fully green on Windows (it is expected to be: the defect is Linux-only)
  - **Done.** Branch from `main` at `7a2c4bc` (pushed, 0/0). Baseline: unit **1951**, integration **183**, rendering **1168**, client **335**, 0 warnings; `SiteSettingsTests` green on Windows, as expected.

## 2. The rule (design D1, D2)

- [x] 2.1 Extract `internal static bool TryGetUsablePolicyLink(string? value, out string? link)` in `UBookItPersistenceComposer` with the D1 order (trim → blank → control/backslash → `/` branch → absolute http(s)); `ResolvePrivacyPolicyUrl` calls it. Keep the existing explanatory comments with the code they explain, and add one at the `/` branch stating why it precedes parsing (the Linux `file:` reading, with the CI run id). Verify: `SiteSettingsTests` green, unchanged
  - **Done.** `TryGetUsablePolicyLink(string?, out string?)`, internal, `[NotNullWhen(true)]`; `ResolvePrivacyPolicyUrl` is now a one-line call. Existing comments kept beside their code; the backslash comment reworded to past tense (its "refused on Windows only by accident" described the old order); new comment at the `/` branch cites CI run `35977150698`. **Escape trap hit and caught during apply:** the Python rewrite emitted the backslash char literal `'\\'` as `'\'` (an unterminated char literal) — the heredoc-escape trap in memory; found by reading the diff, fixed with the Edit tool, verified byte-identical to the original literal and zero control characters in the file. `SiteSettingsTests` green, unchanged.
- [x] 2.2 `SettingValidation`'s `Url` case calls the rule; refusal message "Must be an http or https address, or a site-relative path beginning with a single '/'."; replace the "mirrors the resolver" comment with one saying it calls the resolver's rule. Verify by build + the tests in 3
  - **Done.** The `Url` case calls the rule; message *"Must be an http or https address, or a site-relative path beginning with a single '/'."*; the "mirrors the resolver" comment replaced with one saying it CALLS the rule and why a mirror drifts.

## 3. Tests (design D3)

- [x] 3.1 Move the two resolver theories' data to shared `TheoryData` members so the seam test reads the same source; verify the resolver theories still report the same case count as before
  - **Done.** `SiteSettingsTests.UsablePolicyLinks` / `UnusablePolicyLinks` (`TheoryData<string>`, public static), carrying the original comments. Case count unchanged: **4 + 16 = 20**, as before. Escaped literals (`\t`, `\n`, `\r`, `\u0000`, `\\`) moved with the Edit tool and read back in the diff.
- [x] 3.2 Seam test: for every value in both sets plus a blank, `SettingValidation.IsValid(<PrivacyPolicyUrl descriptor>, value)` equals "the resolver yields a link". Mutation: restore the validator's absolute-only check → red on `/privacy` and `/legal/privacy-policy` on Windows. Mutation: restore the resolver's old order → green on Windows (expected — record it; it is why 5.1 exists)
  - **Done — placed in `SettingsControllerTests`, not a new `SettingValidationTests`**, so it runs through the real endpoint (`PutSetting`) and the real store-over-configuration resolution rather than calling the validator directly. **20 cases** (both shared lists). "Would the site use it" is asked of *configuration*, never of what the screen stored: a refused value is not stored, so asking the store would make every refusal agree vacuously. For accepted values it further asserts the stored value resolves to the same link through `EffectiveConfiguration`. Mutation 1 — validator back to absolute-only → **red on exactly `/privacy` and `/legal/privacy-policy`**, on Windows. Mutation 2 — resolver back to parse-first order → **120/120 green on Windows**, as predicted; that is why 5.1 exists (and 5.1 pushes this mutant through CI to prove Linux sees it).
- [x] 3.3 Test that the refusal message names both forms; and that `/privacy` is accepted by the settings endpoint end to end (via `SettingsControllerTests`' harness), stored, and read back as the effective value
  - **Done.** `The_policy_link_refusal_names_both_accepted_forms` (asserts on `ProblemDetails.Detail`). End-to-end acceptance is the seam test's accepted branch, extended with the screen's read-back (`EffectiveValue` equals the resolved link). That assertion was mutation-checked: `SettingText` made to report `null` for this key → **4 red** (the four usable cases); restored. **Corrected after QA round 1:** those 4 were counted with the test filter restricted to the seam test (`~The_screen_accepts_a_policy_link`). Over the whole settings suites the same mutation gives **5 red**: the four seam cases plus the existing `A_setting_with_nothing_configured_beneath_it_reports_no_configured_value`. The record now names the filter it was taken under.
- [x] 3.4 Mutation-check each refusal family still holds at the seam: temporarily drop the backslash rule → the seam test and `An_unusable_policy_link_resolves_to_none` go red; restore
  - **Done, and the prediction was half wrong — recorded rather than smoothed over.** Backslash rule dropped → **1 red**: `An_unusable_policy_link_resolves_to_none("/\evil.example/x")`. The **seam test stayed green**, and it should: both sides now call one rule, so a mutated rule changes both together and they still agree. The seam test guards *agreement*; the resolver theories guard *the rule*. The task's expectation conflated them. (`\\evil.example\x` stays refused without the rule because it does not start with `/` and parses as a `file:` UNC URI on Windows.) Restored.

## 4. Prose

- [x] 4.1 Outward grep for the rule's vocabulary (`site-relative`, `absolute http`, `http or https`, `PrivacyPolicyUrl`, `mirrors the resolver`, `policy link`) across `docs/`, `openspec/specs/`, `src/`, `tests/`, `README.md`, the client dictionary; record each hit's verdict. `docs/backoffice.md`'s "An absolute `https://` URL or a site-relative path" is expected to be made true, not changed
  - **Done.** Hits and verdicts: `docs/backoffice.md:288,293` (`"/privacy"`, "absolute https URL or a site-relative path") — **made true** on Linux, unchanged; `docs/configuration.md:48,146,324` — state no forms, true; `RunUBookItMigrations.cs:193` startup message listing both forms — **made true** on Linux; `persistence/spec.md:302-355` — says unusable ⇒ absent and **defers to `privacy-notice` for the meaning**, so the new requirement is correctly placed and not a second definition; `privacy-notice/spec.md` scenarios — consistent; `SettingCatalogue.cs:43` `SettingValueKind.Url` summary "An absolute http or https URL." — **FALSIFIED, rewritten**; `en-us.ts:611-613` label/description — state no forms, true; packaging spec / `VersionTruthTests` "absolute https" — readme links, unrelated; rendering/unit fixtures setting `/privacy` directly on `SiteBookingSettings` — bypass resolution, unaffected.
- [x] 4.2 `openspec validate --all --strict` passes; the change's ADDED requirement does not restate or contradict *The package states only what it can keep true, and the site links its own policy* (read both side by side and record)
  - **Done.** Pinned CLI: 26 passed. Side by side: the ADDED requirement refers to *The package states only what it can keep true…* for "treated as absent" rather than restating it, and defines only what that requirement leaves undefined ("usable"). No contradiction. Full CI-mode Release build `--no-incremental`: 0 warnings, 0 errors; unit **1972** (1951 + 20 seam + 1 message), integration 183, rendering 1168.

## 5. Linux proof (design D4)

- [x] 5.1 Rebase `continuous-integration` onto `usable-privacy-link` and push it; record the run URL; the three `SiteSettingsTests` cases that failed in run `35977150698` must now pass, the results check must be green, and the whole run must be green. If anything else fails, classify it per the CI change's D8 before proceeding
  - **Done.** `continuous-integration` rebased onto this branch (`583f3a3`) and force-pushed with a lease on its previous tip `57aeb5a` (pushed by the maintainer — the credential prompt hangs from the agent's shell). **Run `35982930175` — SUCCESS, 2 m 51 s**: every step green, results check green (every project reported, nothing skipped), the three `SiteSettingsTests` cases that failed on Linux in `35977150698` now pass. https://github.com/Chris-N2/UBookIt/actions/runs/35982930175
  - **The guard proved to fire on Linux.** Throwaway branch `ci-proof-parse-first` (`50c900f`) = the CI branch plus this fix reverted to the old parse-first order — the mutant that is **120/120 green on Windows** (3.2). **Run `35983001536` — FAILURE**, and the `UBookIt.Tests` annotation names exactly the three predicted cases: `A_usable_policy_link_is_accepted("/privacy")`, `("/legal/privacy-policy")` (both `Actual: null`) and `A_usable_policy_link_logs_nothing` (the startup error was logged). So the Windows-invisible regression is caught by the Linux run, which is the claim design D3 rests on.
  - **One unrelated failure in the mutant run, classified:** `AvailableDatesCostTests.The_window_read_costs_about_its_width_and_the_number_is_recorded` — "Reading 30 days cost 136.0x reading one … one day 0.013 ms, window 1.726 ms" against a limit of 120x. Not this change (the test reads availability, not settings, and passed on the identical non-mutant code in `35982930175`). ~~An instrument defect in the timing test: its denominator is a ~13 µs average, which on a shared runner is noise.~~ **That classification was premature and is withdrawn.** Measured afterwards: locally the ratio is 5.5–10.5× over 15 runs and a width probe shows a flat ~8–10 µs per day (linear); the runner measured ~57 µs per day in the window against 13 µs for one day. A cost shape that differs by an order of magnitude between platforms is not jitter. Whether it is a slower-but-linear Linux cost (a borderline instrument) or super-linear on Linux (a real finding) is being measured on the runner itself by the throwaway diagnostic branch `ci-probe-available-dates`. Either way it is not this change's code, and it is owned by the `continuous-integration` change. **Reassessed during QA round 3, from the record rather than a measurement:** the deferred obligations already hold this exact test flaking at **195.5× on Windows** in a clean full-suite run (17.0.1, QA round 1), passing in isolation and on the next full run. That makes it **probably** a contention flake under full-suite load rather than a Linux cost shape. "Probably" because the 17.0.1 note itself only says "likely", and the runner's ~57 µs/day is *consistent with* contention, not proven to be it. The same obligations file held a conflicting entry claiming a ratio "cannot flake" because load cancels out. Two measured failures disprove it (load cancels only if it hits both sequential measurements equally), and it has been struck through there. The obligation note says the flake is its own change. The diagnostic branch was unnecessary; checking the obligations first would have found it.

## 6. Handover, then both lines

- [x] 6.1 Handover written here; QA subagent spawned per `CLAUDE.md` with: what is claimed, the CI run URLs to verify, and the instruction to treat each fix as new code
  - **Handover — claims for QA to verify, not trust.** Branch `usable-privacy-link`, reviewed as `git diff main...usable-privacy-link` (the `wip:` commits are squashed at archive). (1) One rule, `UBookItPersistenceComposer.TryGetUsablePolicyLink`, decides a `/`-prefixed value before any `Uri.TryCreate`; `ResolvePrivacyPolicyUrl` and `SettingValidation`'s `Url` case both call it. (2) ~~No value accepted before is refused now; the only widenings are `/…` on Linux and `/…` at the settings screen.~~ **FALSE — QA round 1, MAJOR.** True of the resolver; false of the settings screen, which now also refuses absolute http(s) values containing a control character or backslash that its old check accepted (see proposal *Behaviour changes*). (3) Local: CI-mode Release build `--no-incremental` 0 warnings; unit **1972**, integration **183**, rendering **1168**, client **335**. (4) Mutations and their recorded results are in 3.2–3.4, including one prediction that was wrong (3.4). (5) Linux: run `35982930175` green; mutant run `35983001536` red on exactly the three predicted cases (5.1). Both are public: `https://api.github.com/repos/Chris-N2/UBookIt/actions/runs/<id>` and `…/check-runs/<job id>/annotations` need no login. (6) Every touched file is identical on `main` and `dev/v18` (checked at propose), so 6.2 is a straight cherry-pick.
  - **Known weak points, stated so they are examined rather than discovered:** the Windows suite cannot see a regression of the Linux defect (D3; the Linux run is the guard); the seam test guards agreement, not the rule's correctness (3.4); the escape trap corrupted `tasks.md` twice and a C# literal once during this apply — all repaired, and QA should check the shipped bytes of `UBookItPersistenceComposer.cs`, `SiteSettingsTests.cs` and `SettingsControllerTests.cs` itself rather than take "verified clean" on trust.
  - **QA round 1: REJECT — two MAJOR, one MINOR, two NIT. What was done, each fix treated as new code:**
    - *MAJOR, false "only widens" claim* → proposal *Behaviour changes* rewritten (two widenings, one narrowing, why the narrowing is not a break); non-goal narrowed to "what the SITE refuses"; design Risks and Migration corrected; claim (2) above struck; 6.3 rewritten. **Patch-eligibility of the narrowing: awaiting the maintainer's decision, to be recorded in 6.3.**
    - *MAJOR, one-directional seam* → three rows added to `SiteSettingsTests.UnusablePolicyLinks`: `https:\\evil.example/x` (source `"https:\\\\evil.example/x"`, 4 backslashes in the bytes, verified by a script written with the Write tool), `https://example.com/a<TAB>b` and `https://example.com/p<NUL>x` (source escapes `\t` and `\u0000`; the file holds zero raw control characters and zero tabs). Each also extends the resolver's own theory. **QA's m6** (validator = rule OR old absolute check) → **3 red**, exactly the new rows; **QA's m5** (hand-written mirror with no control/backslash rule on absolute values) → **3 red**, the same rows. Both restored with `git checkout`. Settings suites **126** (was 120).
    - *MINOR, 3.3 record* → corrected above, naming the filter.
    - *NIT, rule remarks* → `TryGetUsablePolicyLink`'s remarks now list all four steps in order, including the control/backslash rule and why `/` precedes parsing.
    - *NIT, `SettingValueKind.Url`* → remarks say the kind carries the policy link's rule only because it is the one such setting, and a second `Url` setting must decide deliberately.
  - **QA round 2: REJECT — one MAJOR, one MINOR, two NIT. What was done, each fix treated as new code:**
    - *MAJOR, design claimed the seam asserted padded agreement; no padded value existed* → `SettingsControllerTests.PaddedPolicyLinks` (`" /privacy "`, `" https://example.com/privacy "`), seam-only because the shared usable list asserts the resolved link equals the configured text. **QA's m7** (validator additionally refuses `value != value.Trim()`) → **2 red**, exactly the padded rows; restored. The design sentence now says when it became true and why no further direction can be missing: outside the shared rule, the validator has only its blank pre-check (covered by `""`, `"   "`) and the untrimmed raw value (covered by the padded rows). Settings suites **128**.
    - *MINOR, proposal pre-empted the patch decision* → release numbers made conditional on 6.3 (patch if eligible, otherwise `17.3.0`/`18.2.0`) in the proposal's *What Changes* and non-goals, design D4 step 5 and this task list's 6.3; the "not a break" paragraph reframed as "the case offered for … an argument for the maintainer's decision, not the decision", and its "The earlier acceptance was the defect" dropped.
    - *NIT, parse result* → `https:\\evil.example/x` parses to `https://evil.example/x`, corrected in the proposal and in the `SiteSettingsTests` comment.
    - *NIT, "existing refusals are unchanged"* → now "the site's existing refusals … unchanged; the settings screen gains the ones it lacked".
  - **QA round 3: REJECT — three MINOR.**
    - *Round 2's `PaddedPolicyLinks` was inserted between the seam test's doc block and the method*, so the seam test lost its documentation and the property carried two summaries. The fix broke its neighbour. Moved above the seam test's doc block; the seam test's remarks now mention the padded rows and state what the test cannot see (the store's length limit).
    - *`proposal.md` Impact still said "a patch release on each line"* → made conditional on 6.3.
    - *The design's "no further direction can be missing" was scoped wider than it held* → narrowed to the validator's decision. The **2048-character settings column** (`UBookItDbContext.cs:247`, confirmed; no layer checks length on write) means an over-long link the site uses from configuration fails at the database rather than with a 400. Pre-existing, every setting, **not fixed here**. The ADDED requirement and its "agree on every value" scenario are bounded to "within the settings store's capacity", with an italic note saying why; recorded as a deferred obligation. Strict validation 26/26 after the spec edit; CI-mode build 0 warnings; unit 1980.
  - **QA round 4: APPROVE WITH NITS** (at `600dfb4`; 7/7 scenarios covered; m1, m5, m6, m7 each red on exactly the expected rows; the reviewer re-ran integration and rendering). Nits tidied without re-review, per the standing rhythm: the 5.1 flake record is softened to "probably" and names the conflicting "ratio cannot flake" entry, which has itself been struck as disproved; the design now quotes the spec's actual wording ("within the settings store's capacity"); and the spec note names the number (2048, the `uBookItSetting.Value` column) and no longer points to a deferred-obligations record that lives outside the repository.
- [ ] 6.2 After QA approval and archive: fast-forward `main`; cherry-pick the fix commit(s) to `dev/v18`; rebuild the client after the branch switch; build + all four suites on `dev/v18`; record counts
  - **Unticked at archive by design.** This step happens AFTER the archive, and the archive may not be edited afterwards, so its results are recorded in the `dev/v18` commit message (`fix(usable-privacy-link): cherry-pick to the 18 line`) and in the session handover, not here. Spec sync done before archive by a separate agent: `openspec/specs/privacy-notice/spec.md` +61 −0, the requirement heading and all 7 scenarios present once each, strict validation 26/26.
- [x] 6.3 **Maintainer's decision, 2026-09-24: PATCH releases — `17.2.1` and `18.1.1`.** In his words: *"this is not a break in the sense meant by the documentation around a minor; we're not changing anything about the API or breaking any published interfaces, so I'm happy with this being a patch."* The settings screen's narrowing (refusing absolute http(s) links containing a control character or backslash, which the site never rendered) is therefore stated in the CHANGELOG as a narrowing, and ships in a patch. Record for the release changes (`release-17-2-1`, `release-18-1-1`): the CHANGELOG entry text — (a) Fixed: a site-relative privacy policy link was refused on Linux hosts; (b) Changed: the settings screen accepts a site-relative link; (c) Changed, a NARROWING stated as one: the settings screen refuses absolute http(s) links containing a control character or backslash, which it used to store although the site never rendered them — `SettingValidation.IsValid` public behaviour, no signature change. Plus the maintainer's recorded decision on whether (c) is patch-eligible
