## 1. Branch and baseline

- [x] 1.1 After the maintainer has pushed the proposal commit, create branch `continuous-integration` from `main`; record `main`'s SHA and the local baseline counts (unit / integration / rendering / client) from a clean client build + `dotnet test` + `npm test`, so first-run counts in CI can be compared against them
  - **Done.** Branch from `main` at `218769b` (pushed; 0/0 with origin). Local baseline, clean client build: unit **1951**, integration **183**, rendering **1168**, client **335**; `dotnet build -c Release` 0 warnings.

## 2. Integration fixture required mode (persistence delta)

- [x] 2.1 Add `UBOOKIT_TEST_DB_REQUIRED` to `SqlServerFixture`: exactly `true` turns the connection failure into a thrown exception carrying the existing diagnostic; any other value or absence keeps the skip. Update the class remarks. Verify by running the integration suite locally with `UBOOKIT_TEST_DB` pointed at an unreachable server (e.g. `Server=127.0.0.1,1;…;Connect Timeout=2`): without the flag every test skips; with `true` every test fails naming the variable; with `TRUE`/`1`, behaviour is recorded and matches the spec's "exactly `true`" wording (decide case-sensitivity explicitly and state it in the remarks)
  - **Done.** Case-sensitive (`StringComparison.Ordinal`) — decided and stated in the remarks: the value is set by a machine, and a near-miss spelling behaves visibly like "not set". Whole suite against `Server=tcp:127.0.0.1,1;…`: no flag → `Passed 6, Skipped 183`; `true` → `Failed 183, Passed 6, Skipped 0`, each failure reading "UBOOKIT_TEST_DB_REQUIRED is 'true', so an unreachable database fails rather than skips. No SQL Server reachable … Set the UBOOKIT_TEST_DB environment variable …". Other spellings are covered by 2.2's theory rather than by hand.
- [x] 2.2 Add a test in the integration project covering both halves of the flag (required → failure; not required → skip reason recorded) that does not depend on a reachable server; mutation-check it by inverting the flag test and confirming it goes red. Verify with `dotnet test tests/UBookIt.Tests.Integration`
  - **Done.** `RequiredDatabaseTests` (6 tests: one required-fails, a five-row theory for `null`/`""`/`false`/`TRUE`/`1` skipping). Drives the fixture's real initialisation through an `internal InitializeAsync(connection, required)` overload that the public one delegates to — no process-wide env writes. Mutations: flag test inverted → **6 red**; `Ordinal` → `OrdinalIgnoreCase` → **1 red** (`TRUE`). Restored, verified by grep for the mutant text (0 hits). Correction made during apply: the remarks first said the closed port is "refused rather than timed out"; the measured message on Windows is "The wait operation timed out", so the remark now says it depends on the platform and is bounded by the fixture's connect timeout.
- [x] 2.3 Confirm the reachable-server path is unaffected: full integration suite green on LocalDB with `UBOOKIT_TEST_DB_REQUIRED=true` and the count equal to 1.1's baseline plus 2.2's additions
  - **Done.** LocalDB with `UBOOKIT_TEST_DB_REQUIRED=true`: **189 passed, 0 skipped** = 183 + 6.

## 3. Build policy and toolchain pins

- [x] 3.1 Add `global.json` (`10.0.300`, `rollForward: latestPatch`) and verify `dotnet --version` in the repo root reports a 10.0.3xx SDK and a clean `dotnet build -c Release` succeeds
  - **Done.** `dotnet --version` in the repo root → `10.0.301`; clean Release build green.
- [x] 3.2 Add `NU1901;NU1902;NU1903;NU1904` to `WarningsNotAsErrors` beside `TreatWarningsAsErrors` in `Directory.Build.props`, with a comment naming the audit workflow. Verify with `$env:CI='true'; dotnet build -c Release --no-incremental` → 0 warnings, 0 errors; and prove the exception is scoped to advisories by temporarily introducing an unrelated warning (e.g. an unused variable) and confirming it still fails the CI-mode build, then reverting
  - **Done.** `CI=true dotnet build UBookIt.slnx -c Release --no-incremental` → 0 warnings, 0 errors. Scoping proved with a throwaway probe project inside the repo (so it imports the props; deleted after) referencing `System.Text.Json 8.0.0`: (a) exception in place → `NU1903` reported as **warning**; (b) exception line removed → `error NU1903: Warning As Error`; (c) exception in place, `int unused = 1;` → `error CS0219`. The probe's one other error in (a)/(b), `NU1510` ("will not be pruned"), comes from the probe's own redundant reference, not from the repository. Props restored; `git diff` shows only the +12-line addition.
- [x] 3.3 Verify `dotnet pack` output is byte-identical in content to a pack from before 3.1–3.2 (compare the file lists and `.nuspec` of each package) so the "no package bytes change" claim is measured, not asserted
  - **Done.** Packed with the changes, then with them stashed (`git stash -u`), `bin/Release` deleted before each. All 9 packages (5 `.nupkg`, 4 `.snupkg`): every entry's CRC identical, excluding only `_rels/.rels` and `package/services/metadata/core-properties/<guid>.psmdcp`, which NuGet names with a fresh GUID on every pack and so differ between any two packs of the same commit.

## 4. Results check

- [x] 4.1 Write `scripts/ci/Assert-TestResults.ps1` per design D2: expected projects = `tests/**/*.csproj` referencing `Microsoft.NET.Test.Sdk` (rule stated in the header); fail on a missing TRX, a TRX with zero executed tests, or any `NotExecuted` outcome, printing project, test name and message
  - **Done.** `scripts/ci/Assert-TestResults.ps1`. **Measured finding that shaped it:** a TRX from a run with 183 real xunit skips reports `<Counters notExecuted="0">` and overall outcome `Completed` — the skips exist only as per-result `outcome="NotExecuted"` and as `executed` (6) < `total` (189). A check on the summary counter could never have fired. The script therefore reads every `UnitTestResult` and independently compares executed with total; the header records the measurement. TRX → project attribution is by the `storage` assembly of the `UnitTest` definitions (lower-cased by the host on Windows, compared case-insensitively); a TRX naming ≠1 assembly, or two TRX for one project, is itself a failure. Non-vacuity: zero discovered projects fails.
- [x] 4.2 Prove it can fire, locally, against doctored inputs in the scratchpad (never committed): (a) a TRX with one skipped test, (b) a TRX with zero tests, (c) one expected project's TRX deleted, (d) a clean set. Record each exit code and message here; (a)–(c) must fail naming the cause, (d) must pass
  - **Done.** Inputs under the scratchpad, real where possible (baseline TRX; the skipped TRX from an actual unreachable-server run). (d) clean baseline set → exit 0, `Every expected test project reported, and nothing was skipped.`; (a) real skips → exit 1, **184 problems** = 183 per-result lines (`UBookIt.Tests.Integration: …Created_resource_is_readable_through_the_read_store was NotExecuted - No SQL Server reachable …`) + `executed 6 of 189 tests`; (b) `executed="0"` → exit 1 `UBookIt.Tests executed no tests.`; (c) integration TRX removed → exit 1 `UBookIt.Tests.Integration produced no test results. It was not built, was filtered out, or failed to start.`; extra (e) `executed="1950"` → exit 1 `executed 1950 of 1951 tests.`; extra (f) duplicate TRX → exit 1 `reported in more than one TRX`. With `GITHUB_ACTIONS=true` each failure is emitted as `::error title=Test results::…`.
- [x] 4.3 Confirm `UBookIt.Tests.ThemeFixture` is excluded by the rule and not by name, by temporarily adding a `Microsoft.NET.Test.Sdk` reference to it in a scratch copy and observing the script now expects it
  - **Done.** Scratch copy of `tests/**/*.csproj` with `Microsoft.NET.Test.Sdk` added to ThemeFixture, `-RepositoryRoot` pointed at it, baseline TRX → exit 1 `UBookIt.Tests.ThemeFixture produced no test results.` Against the real tree it is listed `not expected (references no Microsoft.NET.Test.Sdk)`.

## 5. Workflows

- [x] 5.1 Resolve full commit SHAs for `actions/checkout`, `actions/setup-dotnet`, `actions/setup-node` at their current major release tags; record tag → SHA here
  - **Done.** Resolved with `git ls-remote` (all three are lightweight tags — no `^{}` peel exists — so the SHA is the commit): `actions/checkout` v7.0.1 → `3d3c42e5aac5ba805825da76410c181273ba90b1`; `actions/setup-dotnet` v6.0.0 → `a98b56852c35b8e3190ac28c8c2271da59106c68`; `actions/setup-node` v7.0.0 → `820762786026740c76f36085b0efc47a31fe5020`. Each `action.yml` at that SHA read to confirm the inputs used (`fetch-depth`, `persist-credentials`, `global-json-file`, `cache`, `cache-dependency-path`); all run on `node24`.
- [x] 5.2 Write `.github/workflows/ci.yml` per design D1, D2, D5, D6, D7: triggers, `permissions: contents: read`, concurrency, `fetch-depth: 0`, setup-dotnet from `global.json`, setup-node 22 with npm cache, generated + masked SA password, SQL container with a bounded readiness wait, `UBOOKIT_TEST_DB` + `UBOOKIT_TEST_DB_REQUIRED=true` exported, `npm ci` + `npm test` in the client, `dotnet build UBookIt.slnx -c Release`, `dotnet test UBookIt.slnx -c Release --no-build --logger trx --results-directory <dir>`, the results check, the parity step (published lines and PRs into them only), and `npx --yes @fission-ai/openspec@1.13.0 validate --all --strict`. Verify locally that the YAML parses (e.g. `actionlint` via `npx`/docker if available, else a YAML parse) and that no literal password, key or `secrets.` reference appears (`Grep`)
  - **Done.** Deviations from the design, each a refinement found while writing: (1) the parity comparison and the audit parse are scripts under `scripts/ci/` (`Assert-LineParity.ps1`, `Assert-NoVulnerablePackages.ps1`) rather than inline YAML, so each can be shown failing locally — the parity step never runs on a feature branch, so this is the only place it can be exercised before merge; (2) `push` is scoped to `branches: ['**']`, because an unfiltered `push:` also fires on tags and would re-run a release tag's already-verified commit; (3) the SA password is `openssl rand -hex 16` + a fixed complexity suffix, not a `tr | head` pipeline, which can SIGPIPE under the runner's `pipefail`; (4) `persist-credentials: false` on every checkout. `actionlint` 1.7.12 (release binary, scratchpad only): both workflows clean; a deliberately broken copy is reported (`timeout-minutes: forty` → `[syntax-check]`). Credential grep over `.github/workflows/*.yml` for `secrets.`, `Password=`/`PASSWORD=` followed by a literal: the only hit is the generating line.
- [x] 5.3 Write `.github/workflows/audit.yml` per design D4: weekly schedule + `workflow_dispatch`, matrix over `main` and `dev/v18`, `dotnet list … package --vulnerable --include-transitive` with an output parse that fails on findings; header explains why the matrix must not be replaced by the current ref
  - **Done.** Two checkouts per leg: this (default-branch) checkout supplies the script and `global.json`; the line is checked out into `line/`. So a line that has not yet received a script or `global.json` is still audited. Corrected during apply: a comment first claimed `dotnet` would resolve the line's own `global.json` first; it resolves from the working directory, which is this checkout's root — the comment now says so.
- [x] 5.4 Prove the audit parse can fire: in a scratchpad project (not the repo) reference a package version with a known advisory, run the same command and parse, and record the failure; then run it against the repo and record the clean result
  - **Done.** Scratchpad project referencing `System.Text.Json 8.0.0`: the table form `dotnet list package --vulnerable --include-transitive` **exits 0** despite two High advisories (the header's claim, now measured); the script exits 1 naming `GHSA-8g4q-xg66-9fp4` and `GHSA-hh2w-p6rv-4g7w`. Against `UBookIt.slnx`: `audited 10 project(s)`, no advisories, exit 0. Parity script, locally: vs the real `origin/dev/v18` → exit 1 naming `global.json`; with a remote that does not exist → exit 1 `not available in this clone` (a comparison that could not be made is not a pass).
- [x] 5.5 Verify the fixture-exclusion and history needs hold in CI-shaped conditions locally: a fresh `git clone --depth 1` of the branch fails `RetiredClaimEvidenceTests` (demonstrating why `fetch-depth: 0` is needed), and a full clone passes it. Record both
  - **Done.** `git clone --depth 1` of the branch, then `dotnet test --filter RetiredClaimEvidence` → fails loudly: `` `git show d5ee3c96:docs/notifications.md` failed. fatal: invalid object name 'd5ee3c96'`` and three more. In the full working clone the same tests pass (inside 1.1's 1951).

## 6. First runs on Linux (design D8)

- [x] 6.1 Push the branch (the maintainer if the push prompts); record the run URL, duration, and per-project test counts against 1.1's baseline. Every project must appear; integration count must equal the local count
  - **Done.** Runs on the branch (public: `github.com/Chris-N2/UBookIt/actions`): **run 1** `35975864518` (`b7672ef`), **run 2** `35976470822` (`ab99c45`), **run 3** `35977150698` (`57aeb5a`). Duration ≈ **2 m 40 s** end to end (run 3: SQL Server ready ~19 s after start; build ~55 s; tests ~53 s) — no case for caching. From run 1 on: SQL container healthy, client 335 green, **Release build clean on Linux with warnings as errors**, results check green (every project reported, **nothing skipped**), OpenSpec strict green, parity correctly skipped on a feature branch. **Integration 183 and rendering 1168 pass on Linux against the real SQL Server** — the only failures were in `UBookIt.Tests`.
  - **Found and fixed on the way — the job log needs a signed-in repo admin** (`GET …/jobs/{id}/logs` → 403 unauthenticated), so run 1's failures were unreadable from here. `Assert-TestResults.ps1` now also *reports* (does not count) failed tests: one annotation per project, each test's name and message collapsed to one line, because GitHub keeps ≤10 error annotations per step. Annotations are public, so any reviewer can read a failure's cause. Verified locally against a real failing TRX (183 fixture failures → one annotation); regression cases (a)–(f) of 4.2 unchanged.
- [x] 6.2 For every failure, classify as instrument defect or shipped-code defect and record it here with test name and cause. **Any shipped-code defect: stop and bring it to the maintainer before continuing.**
  - **Instrument defect — `RetiredClaimEvidenceTests` (1 test, 9 of 16 claims).** Four cited commits (`567480aa`, `59eed26d`, `cd6c3fb9`, `d5ee3c96`) are on **no branch or tag, local or remote** — pre-rebase copies surviving only as unreachable objects on the dev machine. The guard has only ever been able to pass there; every fresh clone fails it. Not a Linux issue; the first clean clone found it. Fixed in 6.3.
  - **SHIPPED-CODE DEFECT — `SiteSettingsTests.A_usable_policy_link_is_accepted` (`/privacy`, `/legal/privacy-policy`) and `A_usable_policy_link_logs_nothing`.** `UBookItPersistenceComposer.ResolvePrivacyPolicyUrl` calls `Uri.TryCreate(value, UriKind.Absolute, …)` first; on Linux .NET parses a leading-`/` path as an absolute **`file:`** URI, the http(s) allow-list rejects it, and a **site-relative privacy link is treated as absent** (not rendered on the booking form) **and logged as an Error at startup**. Absolute https links are unaffected. Present since `5e04fb7` (privacy-notice), so in every tagged release on both lines (`17.1.1` … `17.2.0`, `18.0.0`, `18.1.0`); same code on `dev/v18`. Swept the class: the only other `UriKind.Absolute` in `src/` is the settings-screen validator, which demands an absolute http(s) address on every platform (behaves identically), and the link builders start from Umbraco's absolute application URL. **Per design D8, apply STOPPED here and the decision went to the maintainer.**
- [x] 6.3 Fix instrument defects by normalising what the instrument reads (line endings, separators, casing, encoding), never by loosening an assertion; for each fix, show the guard still fires by mutating what it guards on Windows locally. Re-push until green
  - **Done. Three things surfaced, and only one was a fix in this change:**
    - **`RetiredClaimEvidenceTests` — instrument defect, FIXED here.** Four cited commits existed only as unreachable local objects. The nine citations were re-pointed to rebased twins that hold the **identical blob** for each cited path (`d5ee3c96→2240302`, `59eed26d→48cdbc5` for backoffice.md and `→6b82fc7` for mvp.md, `cd6c3fb9→ffd2691`, `567480aa→ca18ccd`), each confirmed on all three published branches with `git branch -r --contains`. Guard still fires: citing `docs/mvp.md @ 218769b` (reachable, wrong text) → `1 of 16 retired claims carry evidence the named commit does not hold`. The test's remarks also claimed the commits were "old and on `main`"; that was corrected, and the reachability rule is now stated there.
    - **Job logs unreadable without admin auth — a diagnosability defect in the pipeline, FIXED here.** The results check now also reports failed tests as one public annotation per project (4.1's regression cases re-run unchanged).
    - **The privacy-link failures — a SHIPPED-code defect, NOT fixed here** (design D8: stop, the maintainer decides). The maintainer chose a separate change: `usable-privacy-link`, archived 2026-09-24, on `main` at `327783e` and `dev/v18` at `9cd14ef`, patch releases `17.2.1`/`18.1.1` to follow this change. This branch was rebased onto it.
    - **`AvailableDatesCostTests` failing once (136×, run `35983001536`) — NOT fixed here.** A known flake already in the deferred obligations (195.5× on Windows under full-suite load in 17.0.1). The maintainer decided it is its **own change, after this one**. Recorded rather than masked: a re-run that happens to go green proves nothing about it.
  - **Green on the final base:** run `35999960054` (`d07e110`, this branch rebased onto `main` at `327783e`): **SUCCESS**. https://github.com/Chris-N2/UBookIt/actions/runs/35999960054
- [x] 6.4 Prove the pipeline itself fails when it should, on the pushed branch, each as a throwaway commit reverted afterwards: (a) a test marked skipped → run fails at the results check; (b) the integration project excluded from the solution build → run fails (SolutionIntegrityTests and/or the results check, record which); (c) an unrelated compiler warning → build fails; (d) an OpenSpec strict-validation error → run fails. Record run URLs. Squash or drop these commits before QA so history stays clean
  - **Done, on two throwaway BRANCHES rather than commits on this one** (history untouched, same evidence). Two branches, not one or four, because a build failure stops the test steps and would mask (a), (b) and (d); so (a)+(b)+(d), which fail at three different steps, share one run and (c) runs alone. Each fault was confirmed locally before push.
    - **`ci-proof-a`** (`00e302c`) — run **`36005190302`: FAILURE**, and every fault failed at its own step. **Test step:** `SolutionIntegrityTests.No_project_is_excluded_from_the_solution_build` names the excluded project, (b). **Results check:** "UBookIt.Tests.Integration produced no test results …", (b), plus the skipped test named with its reason, "…No_policy_link_is_configured_by_default was NotExecuted - ci-proof-a: a skipped test must fail the run", and "UBookIt.Tests executed 1979 of 1980 tests", (a). **OpenSpec step:** failed, (d); locally the pinned CLI reported `ADDED "ci-proof-a deliberately has no scenario" must include at least one scenario`. **Answer to (b)'s "record which": BOTH** — `SolutionIntegrityTests` names the cause and the results check names the missing suite. Reproduced locally first: a solution-level `dotnet test` then ran only 2 of the 3 .NET suites and reported them green, the exact shape of the `0e79a03` incident.
    - **`ci-proof-b`** (`f763775`) — run **`36005216311`: FAILURE at the Build step**, `error CS0219: The variable 'ciProofB' is assigned but its value is never used`, (c). **Not predicted, and correct:** the results check (`!cancelled()`) also failed, reporting that all three .NET suites produced no results. So a broken build cannot be misread as "tests passed" either.
    - `continuous-integration` itself at `eb876de` (the 6.3 record): run `36005166440` **SUCCESS**. Both proof branches are to be deleted after this record (locally by the agent; on origin by the maintainer).
- [ ] 6.5 Trigger `audit.yml` with `workflow_dispatch` on the branch and record that both legs ran (`main` and `dev/v18`) and their result
  - **CANNOT be done before merge — the task as written was impossible, and design D4 missed it.** GitHub offers `workflow_dispatch` ("Run workflow") only for a workflow whose file is on the **default branch**. The API confirms it: `GET …/actions/workflows/audit.yml/runs` → **404** while the file exists only on this branch (the `ci` workflow is registered only because push events ran it). This is the same default-branch rule that D4 applied to `schedule` and missed for `workflow_dispatch`. **Moved to post-merge: 10.1.** Until then the audit's only evidence is local (5.4: the parse fires on a vulnerable package; the repository is clean).

## 7. Prose the change falsifies

- [x] 7.1 `docs/publishing.md`: replace "There is no CI, anywhere" with what now runs, where, and what it does not do (no publishing; not required by branch protection; audit schedule and the 60-day inactivity caveat; the transient parity red). Run the full unit suite — the documentation guards watch this file; if one goes red, judge the sentence rather than relaxing the guard
  - **Done (main).** The outstanding-list bullet now reads "CI verifies, but does not yet gate"; a new *Continuous integration* section says what each workflow does and does not do, the transient parity red, the 60-day schedule caveat, and how to reproduce the CI build locally (`CI=true`, `global.json`). The new prose deliberately does not name the feed, so the feed-accounting guard's counts are untouched. Unit suite after the edit: **1951 passed** — no guard watched the old sentence, so its removal tripped nothing (recorded, not treated as coverage).
- [x] 7.2 `SolutionIntegrityTests` remarks: correct "There is no CI on this repository" to state that CI runs it and why the test still lives in a project that runs (it guards the exclusion that a solution-level CI run would itself be blind to)
  - **Done.** Remarks now say CI runs it and why it still belongs in the suite: a solution-level `dotnet test` in CI is blind to an excluded project exactly as a local one is; the results check catches the symptom, this test the cause.
- [x] 7.3 `CLAUDE.md` conventions line: warnings are errors in CI except NuGet vulnerability advisories, which the scheduled audit checks
  - **Done (main).**
- [x] 7.4 Outward grep across `openspec/specs/`, `docs/`, `README.md`, `CHANGELOG.md`, `CLAUDE.md`, `src/`, `tests/` for `CI`, `continuous integration`, `nothing runs unattended`, `no CI`, `LocalDB`, `skip` (in the integration-test sense), `global.json`, `SDK`; record every hit and its verdict (true / made true / rewritten). Grep both the vocabulary of this change and of what it replaces
  - **Done.** `git grep` over specs, docs, README, CHANGELOG, CLAUDE.md, src, tests, props and `.claude/skills` for `CI`, `continuous integration`, `unattended`, `no CI`, `LocalDB`, `global.json`, `UBOOKIT_TEST_DB`, `maintainer's machine`, `nothing builds`. Verdicts: every `unattended` hit is the booking domain (erasure sweep, mail scanners) — unrelated; `Directory.Build.props:5`, `src/UBookIt/UBookIt.csproj:29`, `BookingViewModels.cs:246` ("warnings are errors in CI") — still true, the exception is stated beside the rule; `MoveWriteShapeTests.cs:14` ("fails on the refactor rather than on the next CI") — true, and now describes something that exists; `persistence/spec.md:10` (LocalDB is supported) — unrelated and true; `persistence/spec.md:367` — replaced by this change's delta; README, CHANGELOG and the skills — no hits. Falsified and rewritten: `docs/publishing.md:76`, `SolutionIntegrityTests.cs:25`, `CLAUDE.md:122`.
- [x] 7.5 Guarantee diff of the persistence delta against `openspec/specs/persistence/spec.md`: list every SHALL and scenario of *Integration test coverage on real SQL Server* and record carried / narrowed / dropped for each (expected: all carried; "Unreachable server is visible" narrowed to the not-required case, deliberately)
  - **Done, mechanically then by eye.** Old requirement: 3 SHALL sentences, 2 scenarios. (1) real SQL Server / `UBOOKIT_TEST_DB` defaulting to LocalDB / per-run database created and dropped — **carried verbatim**; (2) the coverage list — **carried verbatim**; (3) "skip with an explicit diagnostic, never silently pass" — **carried verbatim, then narrowed** by an appended clause: unless `UBOOKIT_TEST_DB_REQUIRED` is exactly `true`, in which case fail with the same diagnostic. Scenario *Test database lifecycle* — **identical**; *Unreachable server is visible* — **narrowed** to "and a database is not declared required" (deliberate; stated in the proposal). Added: *fails when a database is required*, *reachable server unaffected by the flag*. Nothing dropped. The diff script reported the two changes it should, so it demonstrably sees the requirement.
- [x] 7.6 `npx openspec validate --all --strict` passes locally with the pinned CLI
  - **Done.** `npx --yes @fission-ai/openspec@1.13.0 validate --all --strict` → 25 passed, 0 failed.

## 8. The other line

- [x] 8.1 Cherry-pick the CI-definition commit(s) (`.github/`, `global.json`, `scripts/ci/`), the fixture change, the props change and the test-remark change onto `dev/v18`; edit `docs/publishing.md` and `CLAUDE.md` per that line's text. Rebuild the client after the branch switch (stale-hash trap), then build + all four suites locally on `dev/v18` and record counts
  - **Done.** The branch was first squashed into three logical commits on top of `main` (tree byte-identical to the pre-squash tip `83da517`): **`f803956`** everything both lines share (`.github/`, `global.json`, `scripts/ci/`, `Directory.Build.props`, all test changes including the `RetiredClaimEvidenceTests` re-pointing), **`a1274ee`** prose (`CLAUDE.md`, `docs/publishing.md`), and `494d530` this change's own records (main only until archive). On `dev/v18`, `f803956` → **`0fb9c7c`** and `a1274ee` → **`adda258`**. **Both applied with no conflicts.** The props hunk sits clear of `<Version>`, which stays `18.1.0`, and the new *Continuous integration* section of `docs/publishing.md` landed after *Status* as on `main`. Verified on `dev/v18` after a clean client rebuild: `dotnet --version` **10.0.301** via `global.json`; client **335**; CI-mode Release `--no-incremental` **0 warnings**; unit **1991**, integration **189** (with `UBOOKIT_TEST_DB_REQUIRED=true`), rendering **1168**; the results check over the real TRX: "Every expected test project reported, and nothing was skipped."; strict validation **24/24**; audit script **10 projects, no advisories**.
- [x] 8.2 Verify parity mechanically: `git diff --exit-code main dev/v18 -- .github global.json scripts/ci` is empty
  - **Done — against `continuous-integration`, not `main`**, because the CI files are not on `main` until this change merges: `git diff --exit-code continuous-integration dev/v18 -- .github global.json scripts/ci` → empty, exit 0. (The task's literal command, run before merge, lists every CI file as added; I ran it first and briefly misread that. It becomes the right command after merge, and 10.2 is its live form.)
- [x] 8.3 Record the Linux-fix commits from 6.3 and confirm each either applies to `dev/v18` or is shown not to be needed there (the lines' tests differ in places)
  - **Done.** The instrument fixes (the `RetiredClaimEvidenceTests` re-pointing, the failed-test annotations) are inside `f803956` → `0fb9c7c`, and applied unchanged; the retired-claim evidence tests pass on `dev/v18` inside its 1991. The one shipped defect went through `usable-privacy-link`, already on `dev/v18` at `9cd14ef`. Nothing Linux-specific was left behind on either line.

## 9. Handover to QA

- [x] 9.1 Write the handover in this file: claims made (counts, run URLs, the fire-proofs from 4.2, 5.4, 6.4), what was classified in 6.2 and why, and what QA should verify rather than trust. Spawn the QA subagent per `CLAUDE.md`
  - **Handover — claims for QA to verify, not trust.** Review `git diff main...continuous-integration` (three commits: `f803956` shared, `a1274ee` prose, the records commit) and `git diff origin/dev/v18...dev/v18` for the other line (`9cd14ef` belongs to `usable-privacy-link`, already archived and reviewed; this change's commits there are `0fb9c7c` and `adda258`).
    1. **Counts.** `main`+CI: unit 1980, integration 189, rendering 1168, client 335. `dev/v18`: unit 1991, integration 189, rendering 1168, client 335. CI-mode Release `--no-incremental`: 0 warnings on both.
    2. **Linux runs** (public; `https://api.github.com/repos/Chris-N2/UBookIt/actions/runs/<id>`, annotations at `…/check-runs/<job id>/annotations`; job logs need admin auth): the first three runs, `35975864518`, `35976470822` and `35977150698`, are the triage in 6.1/6.2. Green: `35982930175`, `35999960054` and `36005166440`. Fire-proofs: `35983001536` (throwaway branch `ci-proof-parse-first`, made during `usable-privacy-link`: that fix reverted to parse-first order. It went red on the three Linux `SiteSettingsTests` cases AND, unrelated, on the `AvailableDatesCostTests` flake at 136×, which is the same run 6.3 cites for the flake), `36005190302` (proof A) and `36005216311` (proof B).
    3. **Local fire-proofs:** 4.2 (results check: six doctored and real TRX cases), 4.3 (the ThemeFixture rule), 5.4 (audit parse on a vulnerable package; the raw table exits 0 on findings; parity against the real remote and a missing ref), 2.2 (the required-flag mutations).
    4. **Triage (6.2/6.3):** one instrument defect fixed here (local-only evidence commits); one diagnosability defect fixed here (the failed-test annotations); one shipped defect routed to `usable-privacy-link` per D8; one known flake left to its own change by the maintainer's decision.
  - **Known weak points — examine these rather than discover them:**
    - **The parity step has never executed on GitHub.** It is skipped on feature branches by design. Its only evidence is the local script (5.4). Its first live firing is 10.2, after merge, where it is EXPECTED red on `main` until `dev/v18` is pushed.
    - **The audit has never run on GitHub.** `workflow_dispatch` needs the file on the default branch (6.5, D4 amended) → 10.1.
    - **The `pull_request` trigger has never fired.** No PR was opened.
    - **The squashed commits have not had their own CI run.** The last pushed CI run is `36005166440` at `eb876de`. Since then: the 6.4/8.x records, the D4 amendment, and a comment-only change to `audit.yml`'s header. The squashed tree is byte-identical to `83da517`, which was never pushed. The next push of this branch (a force-push, since it was squashed) produces the run for the reviewed commits.
    - **`dev/v18`'s commits have never run on Linux.** They cannot be pushed before QA, and the parity step runs only on the lines themselves.
    - **`AvailableDatesCostTests` is a known flake** (deferred obligation; its own change next, by the maintainer's decision). A red run caused by it alone is not this change's defect, but a green run does not prove it fixed.

  - **QA round 1: REJECT — one MAJOR, four MINOR, eight NIT. What was done; each fix is new code:**
    - *MAJOR, concurrency* — the comment said every commit on a published line completes, but GitHub keeps one PENDING run per group and a newer run cancels it (confirmed against GitHub's concurrency docs). **Fixed in the mechanism:** on `main`/`dev/v18` the group is `ci-<ref>-<sha>`, one per commit, so nothing there is queued or cancelled. Other refs stay one group with cancel-in-progress. The spec's first requirement was narrowed to say exactly that, with two new scenarios (quick successive pushes to a published line are each verified; a superseded feature-branch run may be cancelled). Design D7 was amended with the rejected alternative (`queue: max`); `docs/publishing.md`'s two "every push" sentences were corrected. actionlint clean. Not yet observed live: it needs pushes to a published line (10.2).
    - *MINOR, spec "any step skipped"* → narrowed to the four verification activities, with the event-conditional parity step named as the exception.
    - *MINOR, env-var seam* → `RequiredDatabaseEnvironmentTests` drives the fixture's PUBLIC entry point with the real variables, in an xunit v3 collection with `DisableParallelization = true` (so it runs alone), and restores both variables. QA's mutant (the public entry reads `null` instead of the variable) → **1 red** (`…fails_when_the_environment_requires_a_database`). The fixture's remark claiming the internal overload was "the same code path xunit does" was corrected. Integration **191** (189 + 2). *Process slip, caught:* restoring the mutant with `git checkout -- <file>` also discarded that file's uncommitted remark edit; noticed by grep, re-applied, and from here every mutation is committed-first.
    - *MINOR, audit vacuous-clean* → the script now fails unless the report's sources include `https://api.nuget.org/v3/index.json`. Scratch project on `System.Text.Json 8.0.0`: nuget.org source → exit 1, both advisories named; a nuget.config with only a local source → **exit 1**, "consulted no source that provides vulnerability data" (was exit 0, "No known advisories"); the repository → exit 0.
    - *MINOR, results check blind to non-VSTest projects* → expected set widened to any tests/ project referencing a test framework (`Microsoft.NET.Test.Sdk`, `Microsoft.Testing.Platform*`, `xunit*`, `MSTest*`, `NUnit*`, `TUnit*`) or using the `MSTest.Sdk` project SDK. ThemeFixture is still excluded by rule. A scratch tree with an MTP `xunit.v3`-only project and an `MSTest.Sdk` project → both reported "produced no test results", exit 1. The real tree passes.
    - *NITs:* the SQL Server image is pinned by the **schema-2** manifest digest (`sha256:4402d880…`; the registry's default answer is a schema-1 digest that Docker 28 refuses to pull, so that one would have broken every run; the bump procedure is in the comment; proven only by the next CI run). Parity now also covers `Directory.Build.props` minus its `<Version>` line (the only difference between the lines, measured). Checked against real refs: GitHub's `dev/v18` → props named as differing; local `dev/v18` → props equal, and the 4 scripts from this round differ, as they should until re-cherry-picked. The parity set is restated in `ci.yml`, `docs/publishing.md`, design D6, the spec and the script header. Annotation data is escaped (`%`, CR, LF) in one helper; a doctored skip reason with `100%` and a newline comes out as one annotation line with `%25` and `%0A`. The re-pointed citations were lengthened to 12-character SHAs (evidence tests green). The handover's run `35983001536` is now described once, both ways. Section 10 was moved after 9, and 10.3 (a real PR), 10.4 (`dev/v18`'s first Linux run) and 10.5 (delete the proof branches) were added.

  - **QA round 2: APPROVE WITH NITS** (all five NIT; "Must fix before merge: none"). The reviewer re-ran everything on both lines and attacked each round-1 fix; its mutants were caught, and it confirmed the image digest against the registry. Nits tidied without re-review, per the standing rhythm, each checked:
    - parity drops only the FIRST `<Version>` line, so QA's mutant (a second `<Version>` added on one side) now names `Directory.Build.props`, while the clean case does not flag props. The mutant was committed first and reset away;
    - the props annotation carries the real path `Directory.Build.props`, with a separate line saying the version was ignored;
    - the results check detects `MSTest.Sdk` in `<Sdk Name=…/>` element form too (a scratch project in that form is now expected and reported missing; the real tree still passes). Its header states the two known edges: it errs loud on an xunit helper library under `tests/`, and it cannot see a `GlobalPackageReference`;
    - design D6 describes the props comparison precisely;
    - the audit's exact-URL match was tested by QA and fails closed, so no change.
  - **Gate before merge (QA's words: "treat that run as the gate"):** no GitHub run exists for the rewritten commits. The force-push of this branch must produce a green `ci` run, and it will also be the first live pull of the digest-pinned SQL Server image.
  - **Gate PASSED:** run **`36010870721`** on `3e218ca` (the rewritten head), SUCCESS. Every verification step green, including *Start SQL Server* (so the schema-2 digest pulls on Docker 28); parity skipped as designed on a feature branch; zero annotations (the raw API returns `[]`). The maintainer signed off the artifact amendments made during apply (spec narrowing and new scenarios, the parity set, the D4/D6/D7 amendments, 6.5 moved to 10.1) on 2026-09-24. **Spec sync** by a separate agent before archive: new `openspec/specs/continuous-integration/spec.md` (6 requirements, 21 scenarios, byte-identical to the delta below its header); `persistence/spec.md` +10 −2, all inside *Integration test coverage on real SQL Server*; the guarantee diff was re-checked against HEAD (nothing dropped; one scenario narrowed on purpose; two added); strict validation 26/26.
  - **Unticked at archive, by design:** 6.5 (moved to 10.1) and 10.1–10.5 all happen after merge; the archive cannot be edited afterwards, so their results go in the session handover and the `dev/v18` commit.

## 10. After merge (unticked at archive by design — results go in the handover and the dev/v18 commit)

- [ ] 10.1 With the workflows on `main`: run `audit` by hand (Actions → audit → Run workflow, on `main`); record that BOTH legs ran (`main` and `dev/v18`) and their result. This is 6.5, which could not be done before merge
- [ ] 10.2 First push of `main` carrying the workflows: its `ci` run is expected RED at the parity step only (dev/v18 does not have the CI definitions yet) — record it as the parity check's first live firing; then push `dev/v18` with section 8's cherry-pick and re-run `main`'s job; both must be green with parity passing
- [ ] 10.3 Open a real pull request into `main` (any small branch) and record its `ci` run: the `pull_request` trigger and the parity step's `github.base_ref` branch (and its `-Line` argument) have never executed. Close it unmerged if it was only for this
- [ ] 10.4 Record `dev/v18`'s first Linux run after its push: per-suite counts, the results check green, and parity passing against `main`
- [ ] 10.5 Delete the throwaway branches `ci-proof-a` and `ci-proof-b` from GitHub (the maintainer; their results are recorded in 6.4)
