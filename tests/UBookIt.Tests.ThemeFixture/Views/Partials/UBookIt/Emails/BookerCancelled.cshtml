@inherits UBookIt.Web.Emails.UBookItEmailPage<UBookIt.Core.Notifications.BookerMessageModel>
@* Deliberately throws, so a broken template can be told from an absent one. *@
@{ throw new InvalidOperationException("This template is broken on purpose."); }
